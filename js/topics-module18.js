/* ==========================================================================
   TOPIC CONTENT DATA — Module 18: Operating Systems Deep Dive
   ========================================================================== */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }

window.CSFA_RAW_TOPICS.push({
  id: 'm18-processes',
  module: 18,
  title: 'Processes & Threads',
  tagline: 'The OS unit of execution — how programs run, share the CPU, and talk to each other.',
  readMinutes: 7,
  intro: {
    whatItIs: "A process is an instance of a running program — it has its own memory space, file handles, and CPU state. A thread is a lightweight unit of execution within a process; multiple threads share the same memory space but have their own stack and registers. The OS scheduler decides which process/thread runs on the CPU at any given moment.",
    whyItMatters: "Node.js is single-threaded (one JS thread) but uses worker threads for CPU-heavy tasks. Understanding processes and threads explains why race conditions occur, why parallel programming is hard, and how to use tools like cluster mode or worker_threads in Node.",
    whereUsed: "Web servers (multiple worker processes), browser tabs (each is a process in Chrome), background jobs, game loops, real-time systems — any application that does more than one thing at a time.",
    commonMistakes: "Assuming more threads always means faster code. Thread creation has overhead, and shared memory requires synchronization (locks), which can create deadlocks or race conditions that are harder to debug than single-threaded code."
  },
  visual: { caption: "Process vs Thread: processes have isolated memory; threads share heap but have separate stacks", type: "process-thread" },
  examples: [
    { difficulty: "very-easy", title: "Simulating process states", explanation: "A process moves through states: New → Ready → Running → Waiting → Terminated.", code: "const process = { pid: 1234, name: 'node', state: 'Running', memMB: 64 };\nconsole.log(`PID ${process.pid}: ${process.name} [${process.state}] using ${process.memMB}MB`);", language: "javascript", output: "PID 1234: node [Running] using 64MB" },
    { difficulty: "easy", title: "Process list simulation", explanation: "Operating systems maintain a process table listing all running processes.", code: "const processList = [\n  { pid: 1, name: 'init',   cpu: 0.0 },\n  { pid: 42, name: 'chrome', cpu: 12.5 },\n  { pid: 99, name: 'node',   cpu: 3.2 },\n];\nprocessList.forEach(p => console.log(`${p.pid.toString().padStart(4)}  ${p.name.padEnd(8)}  ${p.cpu}%`));", language: "javascript", output: "   1  init      0%\n  42  chrome    12.5%\n  99  node      3.2%" },
    { difficulty: "medium", title: "Race condition demonstration", explanation: "Two 'threads' reading and writing shared state without synchronization can corrupt data.", code: "let balance = 1000;\n// Both threads read 1000, both add 100, result should be 1200 but may not be\nfunction threadA() { const read = balance; balance = read + 100; }\nfunction threadB() { const read = balance; balance = read + 100; }\nthreadA(); threadB();\nconsole.log('Balance (no race here):', balance);\nconsole.log('In a true parallel env, race could produce 1100 instead of 1200');", language: "javascript", output: "Balance (no race here): 1200\nIn a true parallel env, race could produce 1100 instead of 1200" },
    { difficulty: "medium-plus", title: "Simulating round-robin CPU scheduling", explanation: "Round-robin gives each process a fixed time slice (quantum), then moves to the next.", code: "const tasks = [\n  { name: 'A', remaining: 6 },\n  { name: 'B', remaining: 4 },\n  { name: 'C', remaining: 2 },\n];\nconst quantum = 2;\nlet time = 0;\nwhile (tasks.some(t => t.remaining > 0)) {\n  tasks.filter(t => t.remaining > 0).forEach(t => {\n    const run = Math.min(t.remaining, quantum);\n    console.log(`t=${time}: Run ${t.name} for ${run}ms`);\n    t.remaining -= run; time += run;\n  });\n}", language: "javascript", output: "t=0: Run A for 2ms\nt=2: Run B for 2ms\nt=4: Run C for 2ms\nt=6: Run A for 2ms\nt=8: Run B for 2ms\nt=10: Run A for 2ms" },
    { difficulty: "hard", title: "Deadlock simulation", explanation: "Deadlock: two processes each hold a resource the other needs, so both wait forever.", code: "function simulateDeadlock() {\n  const locks = { A: null, B: null };\n  function tryLock(proc, res1, res2) {\n    if (locks[res1] === null) {\n      locks[res1] = proc;\n      console.log(`${proc} acquired ${res1}`);\n      if (locks[res2] !== null) {\n        console.log(`${proc} waiting for ${res2} (held by ${locks[res2]}) → DEADLOCK`);\n        return false;\n      }\n    }\n    return true;\n  }\n  tryLock('P1', 'A', 'B');\n  tryLock('P2', 'B', 'A');\n}\nsimulateDeadlock();", language: "javascript", output: "P1 acquired A\nP2 acquired B\nP1 waiting for B (held by P2) → DEADLOCK" },
    { difficulty: "real-world", title: "Node.js Worker Threads", explanation: "Node.js worker_threads allow CPU-intensive tasks to run in parallel without blocking the event loop.", code: "// Conceptual example (actual Worker needs a file path)\nconst os = require ? require('os') : { cpus: () => Array(4) };\nconst cpuCount = os.cpus().length;\nconsole.log(`Available CPU cores: ${cpuCount}`);\nconsole.log(`Can spawn up to ${cpuCount} worker threads for parallel CPU work`);\nconsole.log('Use: const { Worker } = require(\"worker_threads\");");", language: "javascript", output: "Available CPU cores: 4\nCan spawn up to 4 worker threads for parallel CPU work\nUse: const { Worker } = require(\"worker_threads\");" }
  ],
  exercises: [
    { level: 1, title: "Process table", problem: "Create an array of 4 process objects each with pid, name, and state ('Running', 'Waiting', or 'Ready'). Print only the Running processes.", hints: ["Use .filter() to get running.", "Print with .forEach()."], solution: "const procs = [{pid:1,name:'A',state:'Running'},{pid:2,name:'B',state:'Waiting'},{pid:3,name:'C',state:'Running'},{pid:4,name:'D',state:'Ready'}];\nprocs.filter(p=>p.state==='Running').forEach(p=>console.log(p.pid, p.name));" },
    { level: 2, title: "Round-robin scheduler", problem: "Implement `roundRobin(tasks, quantum)` where tasks is an array of `{name, time}` objects. Return an array of strings showing which task ran each slice until all complete.", hints: ["Loop until all tasks have time === 0.", "Each iteration reduce task.time by quantum (or to 0)."], solution: "function roundRobin(tasks, quantum) {\n  const log = [];\n  while (tasks.some(t=>t.time>0)) {\n    tasks.filter(t=>t.time>0).forEach(t => { const s=Math.min(t.time,quantum); log.push(`${t.name}(${s}ms)`); t.time-=s; });\n  }\n  return log;\n}\nconsole.log(roundRobin([{name:'A',time:5},{name:'B',time:3}],2));" },
    { level: 3, title: "Deadlock detector", problem: "Given a resource allocation: `held = {P1: 'A', P2: 'B'}` and `waiting = {P1: 'B', P2: 'A'}`, write a function that detects if a deadlock exists by checking if each process is waiting for a resource held by another process that is waiting.", hints: ["Check if held[p1] === waiting[p2] for all pairs.", "If mutual wait found, deadlock exists."], solution: "function detectDeadlock(held, waiting) {\n  const procs = Object.keys(held);\n  for (const p1 of procs) for (const p2 of procs) {\n    if (p1!==p2 && held[p1]===waiting[p2] && held[p2]===waiting[p1]) return `Deadlock: ${p1} ↔ ${p2}`;\n  }\n  return 'No deadlock';\n}\nconsole.log(detectDeadlock({P1:'A',P2:'B'},{P1:'B',P2:'A'}));" }
  ],
  interview: [
    { q: "What is the difference between a process and a thread?", a: "A process is an independent program with its own isolated memory space. A thread is a unit of execution within a process; threads share the process's memory (heap) but have their own stack. Creating a thread is cheaper than creating a process." },
    { q: "What is a race condition?", a: "A race condition occurs when two or more threads access shared state concurrently and the outcome depends on the timing of their execution. Without synchronization (locks/mutexes), one thread may read stale data written by another, causing bugs that are extremely hard to reproduce." },
    { q: "What is deadlock and how do you prevent it?", a: "Deadlock occurs when two processes each hold a resource the other needs and both wait indefinitely. Prevention strategies: (1) Lock ordering — always acquire locks in the same order. (2) Timeouts — give up waiting after a timeout. (3) Resource pre-allocation — allocate all resources at once." }
  ],
  realWorld: [
    { company: "Google Chrome", text: "Chrome uses separate OS processes for each tab, so a crashed tab doesn't crash the whole browser. This process isolation comes at memory cost but provides fault isolation and security sandboxing." },
    { company: "Nginx", text: "Nginx uses a master process and multiple worker processes (one per CPU core). Each worker is single-threaded and handles thousands of connections via async I/O — demonstrating process-level parallelism without threads." }
  ],
  quiz: [
    { q: "What do threads within the same process share?", options: ["Stack memory only", "Registers only", "Heap memory", "Nothing"], answer: 2 },
    { q: "What scheduling algorithm gives each process a fixed time slice?", options: ["FIFO", "Priority Queue", "Round-Robin", "Shortest Job First"], answer: 2 },
    { q: "What is a race condition?", options: ["CPU running too fast", "Two threads accessing shared data with unpredictable timing", "Process waiting for I/O", "Thread spawning too many children"], answer: 1 },
    { q: "What causes a deadlock?", options: ["Too many threads", "Circular resource dependency between processes", "Memory overflow", "CPU overheating"], answer: 1 },
    { q: "Node.js is primarily:", options: ["Multi-threaded", "Single-threaded with an event loop", "Multi-process only", "GPU-accelerated"], answer: 1 }
  ]
});

window.CSFA_RAW_TOPICS.push({
  id: 'm18-deadlocks',
  module: 18,
  title: 'Scheduling & Deadlocks',
  tagline: 'How the OS fairly shares the CPU — and what happens when processes freeze each other.',
  readMinutes: 6,
  intro: {
    whatItIs: "CPU scheduling determines which process runs next. Common algorithms: FIFO (first come, first served), Shortest Job First (SJF), Round-Robin (time slices), and Priority Scheduling. Deadlock is when a set of processes are all waiting for resources held by each other, causing permanent blocking. The OS must detect, prevent, or recover from deadlocks.",
    whyItMatters: "Scheduling directly affects application responsiveness and throughput. In cloud environments, understanding scheduling helps you reason about latency spikes and CPU throttling. Deadlocks in application-level code (database locks, mutex locks) are a real cause of production incidents.",
    whereUsed: "OS kernel schedulers, database lock managers, distributed systems (distributed deadlocks), multi-threaded server applications, real-time embedded systems.",
    commonMistakes: "Using nested locks in the wrong order, causing deadlocks. Always acquire locks in a consistent global order across all threads to prevent circular wait."
  },
  visual: { caption: "Coffman Conditions for Deadlock: Mutual Exclusion + Hold & Wait + No Preemption + Circular Wait", type: "conditions-diagram" },
  examples: [
    { difficulty: "very-easy", title: "FIFO scheduling", explanation: "First-In-First-Out: processes are served in arrival order.", code: "const queue = ['Process A', 'Process B', 'Process C'];\nwhile (queue.length) {\n  const p = queue.shift();\n  console.log('Running:', p);\n}", language: "javascript", output: "Running: Process A\nRunning: Process B\nRunning: Process C" },
    { difficulty: "easy", title: "Priority scheduling", explanation: "Higher-priority processes run first regardless of arrival order.", code: "const tasks = [\n  { name: 'Low priority task',  priority: 1 },\n  { name: 'Critical task',      priority: 10 },\n  { name: 'Medium task',        priority: 5 },\n];\ntasks.sort((a, b) => b.priority - a.priority)\n     .forEach(t => console.log(`Running: ${t.name} (P${t.priority})`));", language: "javascript", output: "Running: Critical task (P10)\nRunning: Medium task (P5)\nRunning: Low priority task (P1)" },
    { difficulty: "medium", title: "Shortest Job First (SJF)", explanation: "SJF minimizes average waiting time by always running the shortest remaining job next.", code: "const jobs = [\n  { name: 'Job A', burst: 6 },\n  { name: 'Job B', burst: 2 },\n  { name: 'Job C', burst: 4 },\n];\njobs.sort((a, b) => a.burst - b.burst)\n    .forEach((j, i) => console.log(`Order ${i+1}: ${j.name} (${j.burst}ms)`));", language: "javascript", output: "Order 1: Job B (2ms)\nOrder 2: Job C (4ms)\nOrder 3: Job A (6ms)" },
    { difficulty: "medium-plus", title: "Detecting circular wait", explanation: "Circular wait is one of the four Coffman conditions for deadlock.", code: "const waitsFor = { P1: 'P2', P2: 'P3', P3: 'P1' }; // cycle!\nfunction hasCycle(graph, start) {\n  const visited = new Set();\n  let cur = start;\n  while (cur) {\n    if (visited.has(cur)) return true;\n    visited.add(cur);\n    cur = graph[cur];\n  }\n  return false;\n}\nconsole.log('Circular wait:', hasCycle(waitsFor, 'P1'));", language: "javascript", output: "Circular wait: true" },
    { difficulty: "hard", title: "Banker's algorithm (resource allocation)", explanation: "The Banker's Algorithm checks if granting a resource request would leave the system in a safe state.", code: "function isSafe(available, need) {\n  const work = [...available];\n  const finish = need.map(() => false);\n  let changed = true;\n  while (changed) {\n    changed = false;\n    need.forEach((n, i) => {\n      if (!finish[i] && n <= work) {\n        work[0] += available[0]; // simplified: reclaim resource\n        finish[i] = true;\n        changed = true;\n      }\n    });\n  }\n  return finish.every(Boolean);\n}\nconsole.log('Safe state:', isSafe([3], [1, 2, 2]));", language: "javascript", output: "Safe state: true" },
    { difficulty: "real-world", title: "Database lock ordering", explanation: "Databases use lock ordering to prevent deadlocks when transactions lock multiple rows or tables.", code: "// Rule: always lock resources in alphabetical order\nfunction acquireLocks(resources) {\n  const ordered = [...resources].sort();\n  console.log('Acquiring locks in order:', ordered);\n  return ordered;\n}\n// Both transactions sort locks the same way → no deadlock\nconst tx1 = acquireLocks(['users', 'accounts']);\nconst tx2 = acquireLocks(['accounts', 'users']);\nconsole.log('Tx1 and Tx2 will never deadlock:', JSON.stringify(tx1) === JSON.stringify(tx2));", language: "javascript", output: "Acquiring locks in order: [ 'accounts', 'users' ]\nAcquiring locks in order: [ 'accounts', 'users' ]\nTx1 and Tx2 will never deadlock: true" }
  ],
  exercises: [
    { level: 1, title: "Sort by priority", problem: "Given tasks = [{name:'A',priority:3},{name:'B',priority:7},{name:'C',priority:1}], sort them highest-to-lowest priority and print in order.", hints: ["Sort with (a,b) => b.priority - a.priority."], solution: "const tasks=[{name:'A',priority:3},{name:'B',priority:7},{name:'C',priority:1}];\ntasks.sort((a,b)=>b.priority-a.priority).forEach(t=>console.log(t.name));" },
    { level: 2, title: "Circular wait detector", problem: "Given an object `waitsFor` where keys are process names and values are the process they're waiting for, write `detectCycle(waitsFor)` that returns true if there's a circular dependency.", hints: ["Follow the chain from each process.", "If you visit the same process twice, there's a cycle."], solution: "function detectCycle(g) {\n  return Object.keys(g).some(start => {\n    const seen = new Set(); let cur = start;\n    while (cur && g[cur]) { if (seen.has(cur)) return true; seen.add(cur); cur = g[cur]; }\n    return false;\n  });\n}\nconsole.log(detectCycle({P1:'P2',P2:'P1'})); // true\nconsole.log(detectCycle({P1:'P2',P2:'P3'})); // false" },
    { level: 3, title: "Average wait time", problem: "Calculate the average waiting time for FIFO scheduling given arrival time 0 for all and burst times [5, 3, 8, 2]. Waiting time for each = sum of burst times of all previous jobs.", hints: ["Waiting time[0]=0. Waiting time[i] = waiting[i-1] + burst[i-1].", "Average = sum(waits) / n."], solution: "const bursts = [5,3,8,2];\nconst waits = bursts.reduce((acc,b,i)=>{ acc.push(i===0?0:acc[i-1]+bursts[i-1]); return acc; },[]);\nconsole.log('Waits:', waits);\nconsole.log('Avg:', waits.reduce((s,w)=>s+w,0)/waits.length);" }
  ],
  interview: [
    { q: "What are the four Coffman conditions for deadlock?", a: "1. Mutual Exclusion — resources can't be shared. 2. Hold and Wait — processes hold resources while waiting for others. 3. No Preemption — resources can't be forcibly taken. 4. Circular Wait — a circular chain of processes each waiting for the next. All four must hold for deadlock to occur." },
    { q: "What is the difference between deadlock and livelock?", a: "In deadlock, processes are completely blocked. In livelock, processes are not blocked — they're actively changing state in response to each other, but making no actual progress (like two people in a hallway both stepping aside the same direction repeatedly)." },
    { q: "How does the OS detect deadlock?", a: "Common approach: resource allocation graph. If the graph has a cycle, deadlock exists. The OS then resolves it by killing one of the processes (victim selection) or rolling back transactions to release resources." }
  ],
  realWorld: [
    { company: "PostgreSQL", text: "PostgreSQL automatically detects deadlocks between transactions using a wait-for graph. When detected, it kills one transaction with 'ERROR: deadlock detected', forcing the application to retry — a real scenario developers must handle." },
    { company: "Linux Kernel", text: "The Linux kernel's lock validator (lockdep) tracks lock acquisition order at runtime and warns when code could potentially cause a deadlock — a sophisticated automated deadlock prevention tool built into the OS itself." }
  ],
  quiz: [
    { q: "Which scheduling algorithm minimizes average waiting time?", options: ["FIFO", "Round-Robin", "Shortest Job First", "Priority"], answer: 2 },
    { q: "Which is NOT a Coffman condition?", options: ["Circular Wait", "Hold and Wait", "Preemption", "Mutual Exclusion"], answer: 2 },
    { q: "What is livelock?", options: ["Processes blocked permanently", "Processes actively running but making no progress", "Stack overflow", "Memory leak"], answer: 1 },
    { q: "How can deadlock be prevented in databases?", options: ["Use faster CPUs", "Always acquire locks in a consistent order", "Use more threads", "Avoid transactions"], answer: 1 },
    { q: "What does the OS do when it detects a deadlock?", options: ["Restarts the computer", "Kills or rolls back one of the deadlocked processes", "Waits indefinitely", "Increases RAM"], answer: 1 }
  ]
});
