/* Module 30 — Error Handling */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }
window.CSFA_RAW_TOPICS.push({
  id: 'm30-try-catch', module: 30, title: 'Error Handling & Debugging',
  tagline: 'try/catch/finally, custom errors, async error handling — making code that fails gracefully.',
  readMinutes: 6,
  intro: {
    whatItIs: "Error handling is the practice of anticipating, catching, and responding to runtime errors. JavaScript uses try/catch/finally and the Error object. Custom error classes enable typed error handling. In async code, errors in Promises must be caught with .catch() or try/catch inside async functions.",
    whyItMatters: "Unhandled errors crash applications. Proper error handling prevents user-facing crashes, enables meaningful error messages, allows retry logic, and logs actionable errors for debugging.",
    whereUsed: "Every production application: API error handling, database failure recovery, invalid user input, network timeouts, file I/O errors, third-party service failures.",
    commonMistakes: "Catching all errors generically and silently swallowing them. Empty catch blocks hide bugs. Always: log the error, return a meaningful response, or re-throw if you can't handle it."
  },
  visual: { caption: "try { risky code } catch(e) { handle error } finally { always runs }", type: "try-catch-flow" },
  examples: [
    { difficulty: "very-easy", title: "Basic try/catch", explanation: "try block runs code that might throw; catch handles the error if it does.", code: "try {\n  const result = JSON.parse('invalid json {');\n  console.log(result);\n} catch (e) {\n  console.log('Error caught:', e.message);\n}", language: "javascript", output: "Error caught: Unexpected token 'i', \"invalid json {\" is not valid JSON" },
    { difficulty: "easy", title: "finally block", explanation: "finally always runs — even if try throws. Use for cleanup (close file, release lock).", code: "function riskyOperation() {\n  console.log('try: starting');\n  try {\n    throw new Error('Something went wrong');\n  } catch (e) {\n    console.log('catch:', e.message);\n  } finally {\n    console.log('finally: cleanup (always runs)');\n  }\n}\nriskyOperation();", language: "javascript", output: "try: starting\ncatch: Something went wrong\nfinally: cleanup (always runs)" },
    { difficulty: "medium", title: "Custom Error classes", explanation: "Extend Error to create typed, catchable error classes for different failure modes.", code: "class ValidationError extends Error {\n  constructor(field, msg) {\n    super(msg);\n    this.name = 'ValidationError';\n    this.field = field;\n  }\n}\nclass NetworkError extends Error {\n  constructor(status, msg) {\n    super(msg);\n    this.name = 'NetworkError';\n    this.status = status;\n  }\n}\ntry {\n  throw new ValidationError('email', 'Invalid email format');\n} catch (e) {\n  if (e instanceof ValidationError) console.log(`Validation [${e.field}]: ${e.message}`);\n  else console.log('Other error:', e.message);\n}", language: "javascript", output: "Validation [email]: Invalid email format" },
    { difficulty: "medium-plus", title: "Async error handling", explanation: "Errors in async functions bubble up as rejected Promises — must catch with try/catch or .catch().", code: "async function fetchData(url) {\n  if (!url.startsWith('https://')) throw new Error('HTTPS required');\n  return { data: 'ok' };\n}\nasync function main() {\n  try {\n    await fetchData('http://insecure.com');\n  } catch (e) {\n    console.log('Async error:', e.message);\n  }\n}\nmain();", language: "javascript", output: "Async error: HTTPS required" },
    { difficulty: "hard", title: "Error retry logic", explanation: "Retry failed operations with exponential backoff — a real-world resilience pattern.", code: "async function withRetry(fn, maxRetries=3, delayMs=100) {\n  for (let attempt=1; attempt<=maxRetries; attempt++) {\n    try {\n      return await fn();\n    } catch(e) {\n      if (attempt===maxRetries) throw e;\n      console.log(`Attempt ${attempt} failed. Retrying...`);\n      await new Promise(r => setTimeout(r, delayMs * attempt));\n    }\n  }\n}\nlet calls=0;\nawait withRetry(async()=>{ if(++calls<3) throw new Error('fail'); return 'success'; })\n  .then(r=>console.log('Result:', r));", language: "javascript", output: "Attempt 1 failed. Retrying...\nAttempt 2 failed. Retrying...\nResult: success" },
    { difficulty: "real-world", title: "Global error boundary", explanation: "Catch all unhandled errors at the top level to prevent silent crashes.", code: "// Browser: window.onerror / unhandledrejection\n// Node.js equivalent:\nprocess.on?.('uncaughtException', err => {\n  console.error('Uncaught:', err.message);\n  process.exit(1);\n});\nprocess.on?.('unhandledRejection', (reason) => {\n  console.error('Unhandled rejection:', reason);\n});\nconsole.log('Global error handlers registered');\nconsole.log('All unhandled errors will now be logged');", language: "javascript", output: "Global error handlers registered\nAll unhandled errors will now be logged" }
  ],
  exercises: [
    { level: 1, title: "Safe JSON parse", problem: "Write `safeJSON(str)` that tries to parse JSON and returns the object on success, or null on failure.", hints: ["Wrap JSON.parse in try/catch, return null in catch."], solution: "function safeJSON(s){try{return JSON.parse(s);}catch{return null;}}\nconsole.log(safeJSON('{\"a\":1}'),safeJSON('bad'));" },
    { level: 2, title: "Custom error", problem: "Create class `AppError(code, message)` extending Error, setting this.code. Throw and catch it, logging `[code]: message`.", hints: ["class AppError extends Error. Set this.code in constructor."], solution: "class AppError extends Error{constructor(code,msg){super(msg);this.code=code;}}\ntry{throw new AppError('NOT_FOUND','User not found');}catch(e){console.log(`[${e.code}]: ${e.message}`);}" },
    { level: 3, title: "Retry wrapper", problem: "Write `retry(fn, times)` that calls async fn up to `times` times, returning the result or throwing on final failure.", hints: ["for loop 1..times. try/await fn(). If last iteration, rethrow."], solution: "async function retry(fn,times){for(let i=1;i<=times;i++){try{return await fn();}catch(e){if(i===times)throw e;}}}\nlet n=0;\nretry(async()=>{if(++n<3)throw new Error('fail');return 'ok';},5).then(console.log);" }
  ],
  interview: [
    { q: "What is the difference between throw and return in error handling?", a: "return exits the function with a value (success or error value). throw exits the function and propagates an Error up the call stack until caught by a try/catch. throw is for exceptional, unexpected conditions; return is for expected error states (like null or {error: ...})." },
    { q: "How do you handle errors in async/await?", a: "Wrap await calls in try/catch. The async function returns a rejected Promise when an unhandled error is thrown, so callers must also catch. Alternatively, use .catch() on the returned Promise." },
    { q: "What is the finally block used for?", a: "finally runs after try/catch regardless of outcome — even if try throws and catch re-throws. Use for cleanup that must always happen: closing database connections, releasing file locks, stopping loading spinners, clearing timeouts." }
  ],
  realWorld: [
    { company: "Sentry", text: "Sentry is a real-time error monitoring service. It captures uncaught exceptions and unhandled rejections from production code, grouping them, showing stack traces, and alerting teams — exactly what global error handlers enable." },
    { company: "AWS SDK", text: "AWS SDK functions throw typed errors (e.g., NoSuchKey for missing S3 objects). Catching specific error types (instanceof NoSuchKey) vs generic errors allows precise retry and fallback logic in production applications." }
  ],
  quiz: [
    { q: "When does the finally block run?", options: ["Only on success", "Only on error", "Always — whether error or success", "Only when explicitly called"], answer: 2 },
    { q: "How do you catch errors in async/await?", options: ["async catch()", ".error()", "try/catch around await", "Promise.catch only"], answer: 2 },
    { q: "What does throwing a custom Error class enable?", options: ["Faster code", "instanceof checks for typed error handling", "Automatic retry", "Logging to console"], answer: 1 },
    { q: "What is wrong with an empty catch block?", options: ["Syntax error", "Errors are silently swallowed — bugs are hidden", "Performance issue", "Nothing wrong"], answer: 1 },
    { q: "What is exponential backoff?", options: ["Reducing retry count over time", "Increasing delay between retries exponentially", "Canceling retries after first failure", "Logging errors to a file"], answer: 1 }
  ]
});
