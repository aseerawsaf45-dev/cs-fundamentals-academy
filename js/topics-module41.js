/* Modules 41-65: Remaining curriculum modules */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }

// MODULE 47 — API Design
window.CSFA_RAW_TOPICS.push({
  id: 'm47-api-design', module: 47, title: 'REST API Design Best Practices',
  tagline: 'Designing APIs that developers love — naming, versioning, pagination, and error formats.',
  readMinutes: 6,
  intro: {
    whatItIs: "Good REST API design follows conventions: resource-based URLs (nouns not verbs), correct HTTP methods, consistent error formats, API versioning, pagination, filtering, and hypermedia. An API is a public contract — once published, breaking changes are very costly.",
    whyItMatters: "Well-designed APIs reduce integration errors, require less documentation, and make clients easier to write. Poor API design creates confusion, bugs, and breaking changes that affect all consumers.",
    whereUsed: "Every web or mobile application with a backend. Public APIs (GitHub, Stripe, Twilio) set the gold standard. Internal microservices also benefit from consistent API design.",
    commonMistakes: "Using verbs in URLs (/getUser, /createPost). Use nouns: /users, /posts. Also: returning 200 OK for errors, inconsistent error formats between endpoints."
  },
  visual: { caption: "GET /users/{id} → 200. POST /users → 201. DELETE /users/{id} → 204. 404/400/500 errors.", type: "api-design" },
  examples: [
    { difficulty: "very-easy", title: "Resource naming conventions", explanation: "URLs should use plural nouns. No verbs. Nested resources for relationships.", code: "const goodUrls = [\n  'GET    /users            → list users',\n  'GET    /users/:id        → get user',\n  'POST   /users            → create user',\n  'PUT    /users/:id        → update user',\n  'DELETE /users/:id        → delete user',\n  'GET    /users/:id/posts  → user\\'s posts',\n];\nconst badUrls = ['/getUser','/createNewPost','/deleteUserById'];\nconsole.log('GOOD:'); goodUrls.forEach(u => console.log(' ', u));\nconsole.log('BAD:', badUrls.join(', '));", language: "javascript", output: "GOOD:\n  GET    /users            → list users\n  GET    /users/:id        → get user\n  POST   /users            → create user\n  PUT    /users/:id        → update user\n  DELETE /users/:id        → delete user\n  GET    /users/:id/posts  → user's posts\nBAD: /getUser, /createNewPost, /deleteUserById" },
    { difficulty: "easy", title: "Consistent error format", explanation: "Standardize error responses so clients can handle them uniformly.", code: "function errorResponse(status, code, message, details = null) {\n  return {\n    error: {\n      status,\n      code,\n      message,\n      ...(details && { details }),\n      timestamp: new Date().toISOString(),\n    }\n  };\n}\nconsole.log(JSON.stringify(errorResponse(400, 'VALIDATION_ERROR', 'Invalid email', { field: 'email' }), null, 2).slice(0,200));", language: "javascript", output: "{\n  \"error\": {\n    \"status\": 400,\n    \"code\": \"VALIDATION_ERROR\",\n    \"message\": \"Invalid email\",\n    \"details\": {\n      \"field\": \"email\"\n    },\n    \"timestamp\": \"2026-06-30T..." },
    { difficulty: "medium", title: "Pagination patterns", explanation: "Offset pagination is simple. Cursor pagination scales better for large datasets.", code: "const offsetPagination = (page=1, limit=20) => ({\n  offset: (page-1)*limit, limit,\n  // Response: { data:[], meta:{ total, page, limit, totalPages } }\n});\nconst cursorPagination = (cursor=null, limit=20) => ({\n  cursor, limit,\n  // Response: { data:[], meta:{ nextCursor, hasMore } }\n});\nconsole.log('Offset:', JSON.stringify(offsetPagination(3, 10)));\nconsole.log('Cursor:', JSON.stringify(cursorPagination('eyJpZCI6MTAwfQ==', 10)));", language: "javascript", output: "Offset: {\"offset\":20,\"limit\":10}\nCursor: {\"cursor\":\"eyJpZCI6MTAwfQ==\",\"limit\":10}" },
    { difficulty: "medium-plus", title: "API versioning strategies", explanation: "Version APIs to evolve without breaking existing clients.", code: "const versionStrategies = [\n  { method: 'URL path',     example: '/v1/users, /v2/users',    pros: 'Simple, explicit', cons: 'Duplicates routes' },\n  { method: 'Header',       example: 'API-Version: 2024-01-01', pros: 'Clean URLs',        cons: 'Less visible' },\n  { method: 'Query param',  example: '/users?version=2',        pros: 'Easy to test',      cons: 'Pollutes URLs' },\n  { method: 'Content-Type', example: 'application/vnd.api+json;v=2', pros: 'REST purist', cons: 'Complex clients' },\n];\nversionStrategies.forEach(v => console.log(`${v.method}: ${v.example}`));", language: "javascript", output: "URL path: /v1/users, /v2/users\nHeader: API-Version: 2024-01-01\nQuery param: /users?version=2\nContent-Type: application/vnd.api+json;v=2" },
    { difficulty: "hard", title: "Rate limiting response headers", explanation: "Rate limiting info in headers enables clients to implement backoff.", code: "function rateLimitHeaders(limit, remaining, resetAt) {\n  return {\n    'X-RateLimit-Limit':     limit,\n    'X-RateLimit-Remaining': remaining,\n    'X-RateLimit-Reset':     Math.floor(resetAt.getTime() / 1000),\n    'Retry-After':           remaining === 0 ? Math.ceil((resetAt - Date.now()) / 1000) : undefined,\n  };\n}\nconst headers = rateLimitHeaders(100, 0, new Date(Date.now() + 30000));\nObject.entries(headers).filter(([,v])=>v!==undefined).forEach(([k,v]) => console.log(`${k}: ${v}`));", language: "javascript", output: "X-RateLimit-Limit: 100\nX-RateLimit-Remaining: 0\nX-RateLimit-Reset: 1751294430\nRetry-After: 30" },
    { difficulty: "real-world", title: "OpenAPI / Swagger spec", explanation: "Document APIs with OpenAPI spec — auto-generates client SDKs and interactive docs.", code: "const openAPIEndpoint = {\n  '/users/{id}': {\n    get: {\n      summary: 'Get user by ID',\n      parameters: [{ name: 'id', in: 'path', required: true, schema: { type: 'integer' } }],\n      responses: {\n        200: { description: 'User found', content: { 'application/json': { schema: { $ref: '#/components/schemas/User' } } } },\n        404: { description: 'User not found' },\n      }\n    }\n  }\n};\nconsole.log('OpenAPI paths:', JSON.stringify(Object.keys(openAPIEndpoint)));\nconsole.log('Summary:', openAPIEndpoint['/users/{id}'].get.summary);", language: "javascript", output: "OpenAPI paths: [\"/users/{id}\"]\nSummary: Get user by ID" }
  ],
  exercises: [
    { level: 1, title: "URL validator", problem: "Write `isGoodApiUrl(url)` returning true if url starts with '/' and does NOT contain verbs: 'get', 'create', 'delete', 'fetch', 'update'.", hints: ["Check url.startsWith('/') and !/(get|create|delete|fetch|update)/i.test(url)."], solution: "const isGoodApiUrl=url=>url.startsWith('/')&&!/(get|create|delete|fetch|update)/i.test(url);\nconsole.log(isGoodApiUrl('/users'),isGoodApiUrl('/getUser'));" },
    { level: 2, title: "Pagination meta builder", problem: "Write `paginationMeta(total, page, limit)` returning {total, page, limit, totalPages, hasNext, hasPrev}.", hints: ["totalPages=Math.ceil(total/limit). hasNext=page<totalPages. hasPrev=page>1."], solution: "function paginationMeta(total,page,limit){const totalPages=Math.ceil(total/limit);return{total,page,limit,totalPages,hasNext:page<totalPages,hasPrev:page>1};}\nconsole.log(paginationMeta(100,3,10));" },
    { level: 3, title: "Error response builder", problem: "Write `apiError(status, code, message)` returning a standardized error object with status, code, message, and ISO timestamp.", hints: ["Return {error:{status,code,message,timestamp:new Date().toISOString()}}."], solution: "const apiError=(status,code,message)=>({error:{status,code,message,timestamp:new Date().toISOString()}});\nconsole.log(apiError(404,'NOT_FOUND','User not found'));" }
  ],
  interview: [
    { q: "What is HATEOAS?", a: "Hypermedia As The Engine Of Application State — a REST constraint where API responses include links to related actions. E.g., a GET /orders/1 response includes links to /orders/1/cancel, /orders/1/track. Clients discover available actions dynamically rather than hard-coding URLs." },
    { q: "What is idempotency in API design?", a: "An idempotent API call produces the same result whether called once or many times. GET, PUT, DELETE are idempotent. POST is not. Idempotency keys (like Stripe's Idempotency-Key header) make POST operations safe to retry after network failures." },
    { q: "How do you handle breaking changes in an API?", a: "Versioning: add /v2 endpoint, keep /v1 working for existing clients. Deprecation period: announce v1 EOL well in advance (typically 6-12 months), send deprecation headers. Add fields (non-breaking), never remove or rename fields in the same version." }
  ],
  realWorld: [
    { company: "Stripe", text: "Stripe's API is considered one of the best-designed APIs in the industry: consistent resource naming, comprehensive error codes, idempotency keys, versioned by date, and complete OpenAPI documentation with type-safe SDKs." },
    { company: "GitHub API", text: "GitHub v3 (REST) and v4 (GraphQL) APIs demonstrate both REST and GraphQL approaches. Their REST API uses hypermedia (Link headers for pagination) and strict resource naming — a real-world example of API design principles." }
  ],
  quiz: [
    { q: "What is correct REST URL for listing users?", options: ["/getUsers", "/user/list", "/users", "/fetch-users"], answer: 2 },
    { q: "What HTTP status code means 'resource created'?", options: ["200", "201", "204", "202"], answer: 1 },
    { q: "What is cursor-based pagination better for than offset?", options: ["Small datasets", "Large datasets that frequently change", "Simple implementations", "Frontend rendering"], answer: 1 },
    { q: "What does API versioning prevent?", options: ["Security issues", "Breaking existing clients when making API changes", "Database errors", "Rate limiting"], answer: 1 },
    { q: "What is idempotency?", options: ["Caching responses", "Same result whether called once or many times", "Authentication method", "API rate limit strategy"], answer: 1 }
  ]
});

// MODULE 48 — GraphQL
window.CSFA_RAW_TOPICS.push({
  id: 'm48-graphql', module: 48, title: 'GraphQL — Query Language for APIs',
  tagline: 'Ask for exactly what you need — typed queries, mutations, subscriptions.',
  readMinutes: 6,
  intro: {
    whatItIs: "GraphQL is a query language and runtime for APIs. Clients specify exactly what data they need in a single request. Schema defines all types and operations. Queries (read), Mutations (write), Subscriptions (real-time). Eliminates REST's over-fetching (too much data) and under-fetching (too many requests).",
    whyItMatters: "GraphQL enables frontend teams to iterate independently — add a new field to a query without waiting for a backend endpoint change. The strongly-typed schema serves as self-documenting contract.",
    whereUsed: "GitHub API v4, Shopify Storefront API, Twitter API, Meta (created by Facebook), Hasura, Apollo Server.",
    commonMistakes: "N+1 problem in GraphQL resolvers — each field resolver queries the DB independently. Fix with DataLoader (batching and caching). Also: over-exposing sensitive fields in the schema."
  },
  visual: { caption: "Query: {user(id:1){name,posts{title}}} → single request, exact data needed", type: "graphql-query" },
  examples: [
    { difficulty: "very-easy", title: "Query vs REST", explanation: "REST requires multiple requests; GraphQL gets everything in one.", code: "// REST approach (multiple requests):\nconsole.log('REST: 3 separate HTTP requests needed:');\nconsole.log('  GET /users/1');\nconsole.log('  GET /users/1/posts');\nconsole.log('  GET /users/1/followers');\nconsole.log('');\nconsole.log('GraphQL: ONE request:');\nconsole.log(`  query {\n    user(id: 1) {\n      name\n      posts { title }\n      followers { name }\n    }\n  }`);", language: "javascript", output: "REST: 3 separate HTTP requests needed:\n  GET /users/1\n  GET /users/1/posts\n  GET /users/1/followers\n\nGraphQL: ONE request:\n  query {\n    user(id: 1) {\n      name\n      posts { title }\n      followers { name }\n    }\n  }" },
    { difficulty: "easy", title: "GraphQL schema definition", explanation: "The schema defines all types and operations — the API contract.", code: "const schema = `\ntype User {\n  id: ID!\n  name: String!\n  email: String!\n  posts: [Post!]!\n  createdAt: String!\n}\n\ntype Post {\n  id: ID!\n  title: String!\n  body: String!\n  author: User!\n}\n\ntype Query {\n  user(id: ID!): User\n  users: [User!]!\n}\n\ntype Mutation {\n  createUser(name: String!, email: String!): User!\n}\n`;\nconsole.log('Schema has', schema.split('\\n').filter(l=>l.trim()).length, 'definition lines');", language: "javascript", output: "Schema has 20 definition lines" },
    { difficulty: "medium", title: "Resolver functions", explanation: "Resolvers provide the data for each field in the schema.", code: "const resolvers = {\n  Query: {\n    user: (_, { id }, ctx) => ctx.db.users.find(u => u.id === id),\n    users: (_, __, ctx) => ctx.db.users,\n  },\n  User: {\n    posts: (user, _, ctx) => ctx.db.posts.filter(p => p.authorId === user.id),\n  },\n  Mutation: {\n    createUser: (_, { name, email }, ctx) => {\n      const user = { id: Date.now().toString(), name, email };\n      ctx.db.users.push(user);\n      return user;\n    },\n  },\n};\nconsole.log('Resolvers defined:', Object.keys(resolvers).join(', '));", language: "javascript", output: "Resolvers defined: Query, User, Mutation" },
    { difficulty: "medium-plus", title: "DataLoader for N+1 prevention", explanation: "DataLoader batches multiple individual DB queries into a single batch query.", code: "class DataLoader {\n  constructor(batchFn) {\n    this.batchFn = batchFn;\n    this.queue = [];\n  }\n  load(key) {\n    this.queue.push(key);\n    return new Promise(resolve => {\n      setTimeout(() => {\n        if (this.queue.length > 0) {\n          const keys = [...this.queue]; this.queue = [];\n          console.log(`Batching ${keys.length} DB calls into 1: ids=[${keys}]`);\n          this.batchFn(keys).then(results => results.forEach(r => resolve(r)));\n        }\n      }, 0);\n    });\n  }\n}\nconst userLoader = new DataLoader(ids => Promise.resolve(ids.map(id => ({id, name:`User${id}`}))));\n[1,2,3].forEach(id => userLoader.load(id).then(u => console.log('Got:', u.name)));", language: "javascript", output: "Batching 3 DB calls into 1: ids=[1,2,3]\nGot: User1\nGot: User2\nGot: User3" },
    { difficulty: "hard", title: "GraphQL subscriptions", explanation: "Subscriptions provide real-time updates via WebSocket when data changes.", code: "const subscriptionExample = `\n# Client subscribes:\nsubscription {\n  messageAdded(roomId: \"room-1\") {\n    id\n    text\n    sender { name }\n    sentAt\n  }\n}\n# Server publishes when new message is created:\n# pubsub.publish('MESSAGE_ADDED', { messageAdded: newMessage, roomId: 'room-1' })\n`;\nconsole.log('GraphQL subscription flow:');\nconsole.log('1. Client connects via WebSocket');\nconsole.log('2. Sends subscription query');\nconsole.log('3. Server pushes events in real-time');\nconsole.log('4. Client receives update without polling');", language: "javascript", output: "GraphQL subscription flow:\n1. Client connects via WebSocket\n2. Sends subscription query\n3. Server pushes events in real-time\n4. Client receives update without polling" },
    { difficulty: "real-world", title: "Apollo Client query", explanation: "Apollo Client in React manages GraphQL queries with caching and loading states.", code: "// Apollo Client usage pattern\nconst apolloUsage = `\nconst GET_USER = gql\\`\n  query GetUser($id: ID!) {\n    user(id: $id) {\n      id name email\n      posts { id title }\n    }\n  }\n\\`;\n\nfunction UserProfile({ userId }) {\n  const { loading, error, data } = useQuery(GET_USER, { variables: { id: userId } });\n  if (loading) return <Spinner />;\n  if (error) return <Error message={error.message} />;\n  return <div>{data.user.name}</div>;\n}\n`;\nconsole.log('Apollo manages: loading, error, data, cache automatically');", language: "javascript", output: "Apollo manages: loading, error, data, cache automatically" }
  ],
  exercises: [
    { level: 1, title: "Schema reader", problem: "Write `countTypes(schema)` that counts type definitions in a GraphQL schema string (count occurrences of 'type ').", hints: ["schema.match(/type /g)?.length || 0"], solution: "const countTypes=s=>(s.match(/type /g)||[]).length;\nconsole.log(countTypes('type User { id: ID } type Post { title: String }'));" },
    { level: 2, title: "Query field extractor", problem: "Given a simple query string like '{user{name,email,posts{title}}}', write `extractTopFields(query)` returning top-level fields after 'user' (['name','email','posts']).", hints: ["Extract inner content of user{...}, split by comma."], solution: "function extractTopFields(q){const m=q.match(/user\\{([^}]+)/);return m?m[1].split(',').map(f=>f.trim()):[];}\nconsole.log(extractTopFields('{user{name,email,posts{title}}}'));" },
    { level: 3, title: "Batch loader", problem: "Write `batchLoad(ids, fetchFn)` that calls fetchFn ONCE with all ids and returns an array of results in the same order.", hints: ["await fetchFn(ids), then map results by id."], solution: "async function batchLoad(ids,fetchFn){const results=await fetchFn(ids);return ids.map(id=>results.find(r=>r.id===id));}\nbatchLoad([1,2],ids=>Promise.resolve(ids.map(id=>({id,name:`U${id}`})))).then(console.log);" }
  ],
  interview: [
    { q: "What are the advantages of GraphQL over REST?", a: "1. No over/under-fetching — get exactly what you need. 2. Single endpoint vs many REST endpoints. 3. Strongly typed schema serves as documentation. 4. Introspection — clients can discover the schema. 5. Evolve API without versioning (just add fields). Disadvantages: complexity, N+1 risk, caching harder than REST." },
    { q: "What is the N+1 problem in GraphQL?", a: "If a query asks for users and each user's posts, naive resolvers issue 1 query for users + N queries (one per user) for posts = N+1 total. DataLoader solves this by batching all post queries for a 'tick' into a single IN query: SELECT * FROM posts WHERE userId IN (1,2,...N)." },
    { q: "What is the difference between Query, Mutation, and Subscription?", a: "Query: read-only data fetching (GET equivalent). Mutation: data modification (POST/PUT/DELETE equivalent) — by convention runs serially. Subscription: real-time stream of events via WebSocket (push, not pull). All are defined in the schema's root operation types." }
  ],
  realWorld: [
    { company: "GitHub", text: "GitHub's API v4 is GraphQL. Developers query exactly the repository fields they need — no over-fetching huge REST payloads. GitHub's GraphQL Explorer lets you build and test queries interactively." },
    { company: "Shopify", text: "Shopify's Storefront API is GraphQL, enabling headless commerce. React + Apollo + Shopify GraphQL is a standard JAMstack e-commerce stack used by thousands of online stores." }
  ],
  quiz: [
    { q: "What problem does GraphQL solve vs REST?", options: ["Authentication", "Over-fetching and under-fetching", "Database connections", "File uploads"], answer: 1 },
    { q: "What is a GraphQL mutation?", options: ["A read-only query", "A data modification operation", "A subscription", "A schema type"], answer: 1 },
    { q: "What does DataLoader solve?", options: ["Schema validation", "N+1 query problem by batching", "Authentication", "Type checking"], answer: 1 },
    { q: "GraphQL subscriptions use which protocol for real-time updates?", options: ["HTTP polling", "Server-Sent Events", "WebSocket", "Long-polling"], answer: 2 },
    { q: "GraphQL schema uses ! to indicate:", options: ["Optional field", "Required/non-null field", "Deprecated field", "List field"], answer: 1 }
  ]
});
