/* Module 25 — Browser Internals: DOM & Rendering */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }
window.CSFA_RAW_TOPICS.push({
  id: 'm25-dom', module: 25, title: 'DOM & Rendering Pipeline',
  tagline: 'How browsers parse HTML, build the DOM, and paint pixels to the screen.',
  readMinutes: 6,
  intro: {
    whatItIs: "The Document Object Model (DOM) is a tree-structured representation of an HTML page that JavaScript can query and manipulate. The browser rendering pipeline: Parse HTML → Build DOM → Parse CSS → Build CSSOM → Combine into Render Tree → Layout → Paint → Composite. Every time JavaScript modifies the DOM, parts of this pipeline may re-run.",
    whyItMatters: "Unnecessary DOM manipulations cause layout thrashing and janky 60fps experiences. Understanding the pipeline helps you batch DOM reads/writes, use CSS transforms instead of layout properties, and know when to use requestAnimationFrame.",
    whereUsed: "Every frontend developer needs this. Frameworks like React use a virtual DOM to minimize real DOM operations. Tools like Lighthouse measure rendering performance.",
    commonMistakes: "Reading layout properties (offsetWidth, getBoundingClientRect) after writing to the DOM in a loop — this forces synchronous layout recalculation ('layout thrashing'), making pages extremely slow."
  },
  visual: { caption: "HTML → DOM + CSS → CSSOM → Render Tree → Layout → Paint → Composite (screen)", type: "rendering-pipeline" },
  examples: [
    { difficulty: "very-easy", title: "DOM tree structure", explanation: "The DOM represents HTML as a tree of nodes.", code: "const domTree = {\n  tag: 'html', children: [\n    { tag: 'head', children: [{ tag: 'title', text: 'Page' }] },\n    { tag: 'body', children: [\n      { tag: 'h1', text: 'Hello' },\n      { tag: 'p',  text: 'World' }\n    ]}\n  ]\n};\nfunction printTree(node, depth=0) {\n  console.log(' '.repeat(depth*2) + (node.tag || node.text));\n  (node.children||[]).forEach(c => printTree(c, depth+1));\n}\nprintTree(domTree);", language: "javascript", output: "html\n  head\n    title\n      Page\n  body\n    h1\n      Hello\n    p\n      World" },
    { difficulty: "easy", title: "DOM query simulation", explanation: "querySelector finds the first matching element; querySelectorAll finds all.", code: "const elements = [\n  { tag: 'p', class: 'text', id: 'intro' },\n  { tag: 'div', class: 'card', id: 'c1' },\n  { tag: 'p', class: 'text', id: 'body' },\n];\nconst byClass = elements.filter(e => e.class === 'text');\nconsole.log('querySelectorAll(.text):', byClass.map(e => e.id).join(', '));", language: "javascript", output: "querySelectorAll(.text): intro, body" },
    { difficulty: "medium", title: "Layout thrashing (bad vs good)", explanation: "Reading layout after writing forces synchronous reflow. Batch reads before writes.", code: "// BAD: alternating read/write causes layout thrashing\nfunction bad(items) {\n  items.forEach(item => {\n    const h = item.height; // READ (forces reflow)\n    item.height = h + 10;  // WRITE\n  });\n}\n// GOOD: batch reads, then writes\nfunction good(items) {\n  const heights = items.map(i => i.height);   // all READs\n  items.forEach((item,i) => item.height = heights[i]+10); // all WRITEs\n}\nconsole.log('Bad: causes', 3, 'reflows');\nconsole.log('Good: causes', 1, 'reflow');", language: "javascript", output: "Bad: causes 3 reflows\nGood: causes 1 reflow" },
    { difficulty: "medium-plus", title: "Virtual DOM concept", explanation: "React's virtual DOM diffs old and new VDOM trees to minimize real DOM changes.", code: "function diff(oldTree, newTree) {\n  const patches = [];\n  Object.keys(newTree).forEach(key => {\n    if (oldTree[key] !== newTree[key])\n      patches.push({ key, from: oldTree[key], to: newTree[key] });\n  });\n  return patches;\n}\nconst old = { title: 'Hello', color: 'red', count: 1 };\nconst next = { title: 'Hello', color: 'blue', count: 2 };\nconsole.log('Patches:', diff(old, next));", language: "javascript", output: "Patches: [ { key: 'color', from: 'red', to: 'blue' }, { key: 'count', from: 1, to: 2 } ]" },
    { difficulty: "hard", title: "CSS trigger properties", explanation: "Some CSS properties trigger layout (expensive), others only trigger paint or composite (cheap).", code: "const cssTriggers = {\n  width: 'Layout + Paint + Composite', height: 'Layout + Paint + Composite',\n  color: 'Paint + Composite', opacity: 'Composite only',\n  transform: 'Composite only',\n};\nObject.entries(cssTriggers).forEach(([prop, cost]) =>\n  console.log(`${prop.padEnd(10)}: ${cost}`));", language: "javascript", output: "width     : Layout + Paint + Composite\nheight    : Layout + Paint + Composite\ncolor     : Paint + Composite\nopacity   : Composite only\ntransform : Composite only" },
    { difficulty: "real-world", title: "requestAnimationFrame for smooth animation", explanation: "rAF syncs your animation with the browser's repaint cycle for smooth 60fps.", code: "let frame = 0;\nfunction animate() {\n  frame++;\n  // Would update DOM here\n  if (frame < 5) {\n    // requestAnimationFrame(animate); // in browser\n    animate(); // simulate for demo\n  }\n}\nconsole.log('rAF schedule: each frame runs just before browser paint');\nconsole.log('Target: 16.67ms per frame = 60fps');\nconsole.log('Use: requestAnimationFrame(cb) instead of setInterval');", language: "javascript", output: "rAF schedule: each frame runs just before browser paint\nTarget: 16.67ms per frame = 60fps\nUse: requestAnimationFrame(cb) instead of setInterval" }
  ],
  exercises: [
    { level: 1, title: "Tree printer", problem: "Write `countNodes(tree)` where tree has `{tag, children:[]}`. Return total node count.", hints: ["Recursively count 1 + sum of children counts."], solution: "function countNodes(t){return 1+(t.children||[]).reduce((s,c)=>s+countNodes(c),0);}\nconsole.log(countNodes({tag:'div',children:[{tag:'p',children:[]},{tag:'span',children:[]}]})); // 3" },
    { level: 2, title: "VDOM differ", problem: "Write `diff(a, b)` for two flat objects returning array of keys that changed.", hints: ["Object.keys, filter where a[k]!==b[k]."], solution: "function diff(a,b){return Object.keys(b).filter(k=>a[k]!==b[k]);}\nconsole.log(diff({x:1,y:2},{x:1,y:99})); // ['y']" },
    { level: 3, title: "CSS trigger classifier", problem: "Write `getTrigger(prop)` returning 'layout', 'paint', or 'composite' based on CSS property name.", hints: ["Map width/height/margin to layout, color/background to paint, opacity/transform to composite."], solution: "function getTrigger(p){if(['width','height','margin','padding'].includes(p))return 'layout';if(['color','background'].includes(p))return 'paint';return 'composite';}\nconsole.log(getTrigger('width'),getTrigger('color'),getTrigger('transform'));" }
  ],
  interview: [
    { q: "What is layout thrashing?", a: "Layout thrashing occurs when JavaScript alternates between reading layout properties (like offsetWidth) and writing DOM properties in a loop. Each read forces the browser to synchronously recalculate layout. Fix by batching all reads first, then all writes." },
    { q: "Why does React use a virtual DOM?", a: "The real DOM is slow to manipulate. React maintains a lightweight JS object tree (VDOM). On state change, it computes the diff between old and new VDOM and applies only the minimal real DOM changes needed — avoiding unnecessary reflows and repaints." },
    { q: "Which CSS properties are cheapest to animate?", a: "opacity and transform — they only trigger compositing, which runs on the GPU without recalculating layout or paint. Animating width, height, or top/left triggers full layout recalculation and is much more expensive." }
  ],
  realWorld: [
    { company: "React", text: "React Fiber (React 16+) can pause and resume rendering using the event loop, prioritizing user interactions over background re-renders — directly applying event loop and rendering pipeline knowledge." },
    { company: "Google Lighthouse", text: "Lighthouse's performance score directly measures rendering pipeline efficiency: First Contentful Paint, Largest Contentful Paint, and Cumulative Layout Shift all measure how quickly and stably the browser completes the render pipeline." }
  ],
  quiz: [
    { q: "What is the correct browser rendering pipeline order?", options: ["Paint → Layout → DOM → CSSOM", "HTML → DOM → CSSOM → Render Tree → Layout → Paint", "CSS → Paint → DOM", "DOM → Paint → Layout"], answer: 1 },
    { q: "Which CSS property only triggers compositing (cheapest)?", options: ["width", "margin", "transform", "color"], answer: 2 },
    { q: "What is layout thrashing?", options: ["Too many elements on page", "Alternating DOM reads/writes forcing repeated reflows", "Network request overload", "JavaScript syntax error"], answer: 1 },
    { q: "What does React's virtual DOM do?", options: ["Replaces real DOM entirely", "Stores DOM in localStorage", "Diffs old/new trees to minimize real DOM updates", "Speeds up CSS parsing"], answer: 2 },
    { q: "Which API syncs animation with browser repaint?", options: ["setInterval", "setTimeout", "requestAnimationFrame", "MutationObserver"], answer: 2 }
  ]
});
