/* Modules 33–40: Responsive Design through Modules & Tooling */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }

// MODULE 33 — Responsive Design
window.CSFA_RAW_TOPICS.push({
  id: 'm33-media-queries', module: 33, title: 'Responsive Design & Media Queries',
  tagline: 'Building interfaces that adapt to any screen — mobile-first, breakpoints, fluid layouts.',
  readMinutes: 6,
  intro: {
    whatItIs: "Responsive design creates UIs that work on any device/screen size. Media queries apply CSS rules conditionally based on viewport width, device type, or preferences. Mobile-first means writing styles for mobile by default, then adding breakpoints for larger screens with `min-width` queries.",
    whyItMatters: "Over 60% of web traffic is mobile. Responsive design is not optional. Mobile-first approach ensures the base experience works on the smallest screen and progressively enhances for larger ones.",
    whereUsed: "Every modern website and web app. Google uses mobile-first indexing — responsive design directly impacts SEO. CSS frameworks like Tailwind are built on responsive breakpoints.",
    commonMistakes: "Desktop-first approach — writing desktop styles then overriding for mobile. This leads to heavier CSS on mobile. Mobile-first (min-width queries) is the correct approach."
  },
  visual: { caption: "Mobile (base) → sm:768px → md:1024px → lg:1280px progressively enhanced", type: "breakpoints" },
  examples: [
    { difficulty: "very-easy", title: "Media query syntax", explanation: "Media queries apply styles only when conditions are met.", code: "const mediaQueries = [\n  '@media (max-width: 767px) { /* mobile */ }',\n  '@media (min-width: 768px) { /* tablet+ */ }',\n  '@media (min-width: 1024px) { /* desktop+ */ }',\n  '@media (prefers-color-scheme: dark) { /* dark mode */ }',\n  '@media print { /* print styles */ }',\n];\nmediaQueries.forEach(mq => console.log(mq));", language: "javascript", output: "@media (max-width: 767px) { /* mobile */ }\n@media (min-width: 768px) { /* tablet+ */ }\n@media (min-width: 1024px) { /* desktop+ */ }\n@media (prefers-color-scheme: dark) { /* dark mode */ }\n@media print { /* print styles */ }" },
    { difficulty: "easy", title: "Mobile-first breakpoints", explanation: "Mobile-first: write base styles for mobile, use min-width to enhance for larger screens.", code: "const mobileFirst = `\n/* BASE: mobile styles — no media query */\n.grid { display: grid; grid-template-columns: 1fr; gap: 16px; }\n\n/* TABLET: >= 768px */\n@media (min-width: 768px) {\n  .grid { grid-template-columns: repeat(2, 1fr); }\n}\n\n/* DESKTOP: >= 1024px */\n@media (min-width: 1024px) {\n  .grid { grid-template-columns: repeat(3, 1fr); }\n}\n`;\nconsole.log('Mobile-first grid adapts:', '1 col → 2 col → 3 col');", language: "javascript", output: "Mobile-first grid adapts: 1 col → 2 col → 3 col" },
    { difficulty: "medium", title: "CSS fluid units", explanation: "Fluid units (%, vw, vh, em, rem, clamp) create naturally responsive layouts.", code: "const units = [\n  { unit: '%',    example: 'width: 100%',           desc: 'relative to parent' },\n  { unit: 'vw',   example: 'width: 50vw',           desc: '50% of viewport width' },\n  { unit: 'rem',  example: 'font-size: 1.5rem',     desc: 'relative to root font-size' },\n  { unit: 'clamp', example: 'font-size: clamp(1rem, 2vw, 2rem)', desc: 'fluid between min and max' },\n];\nunits.forEach(u => console.log(`${u.unit.padEnd(6)}: ${u.example.padEnd(35)} → ${u.desc}`));", language: "javascript", output: "%     : width: 100%                         → relative to parent\nvw    : width: 50vw                         → 50% of viewport width\nrem   : font-size: 1.5rem                   → relative to root font-size\nclamp : font-size: clamp(1rem, 2vw, 2rem)   → fluid between min and max" },
    { difficulty: "medium-plus", title: "Container queries (modern CSS)", explanation: "Container queries style elements based on their parent container's size, not the viewport.", code: "const containerQuery = `\n/* Define a containment context */\n.card-wrapper { container-type: inline-size; }\n\n/* Style card based on its container width */\n@container (min-width: 400px) {\n  .card {\n    display: flex;\n    flex-direction: row;\n  }\n}\n/* Card goes horizontal when its container is 400px+ wide */\n/* More powerful than viewport media queries! */\n`;\nconsole.log('Container queries: style by parent size, not viewport');", language: "javascript", output: "Container queries: style by parent size, not viewport" },
    { difficulty: "hard", title: "clamp() for fluid typography", explanation: "clamp(min, preferred, max) creates fluid typography that scales with viewport.", code: "function clampValue(minVal, viewportWidth, maxVal, vwFactor=2) {\n  const fluid = (viewportWidth / 100) * vwFactor;\n  return Math.min(maxVal, Math.max(minVal, fluid));\n}\nconst viewports = [320, 768, 1024, 1440];\nviewports.forEach(vw => {\n  const fontSize = clampValue(16, vw, 24, 2.5);\n  console.log(`Viewport ${vw}px → font-size: ${fontSize.toFixed(1)}px`);\n});", language: "javascript", output: "Viewport 320px → font-size: 16.0px\nViewport 768px → font-size: 19.2px\nViewport 1024px → font-size: 24.0px\nViewport 1440px → font-size: 24.0px" },
    { difficulty: "real-world", title: "Tailwind responsive prefix pattern", explanation: "Tailwind's responsive classes follow mobile-first breakpoint prefixes.", code: "const tailwindResponsive = {\n  'grid-cols-1':    'Default: 1 column (mobile)',\n  'sm:grid-cols-2': 'sm (640px+): 2 columns',\n  'md:grid-cols-3': 'md (768px+): 3 columns',\n  'lg:grid-cols-4': 'lg (1024px+): 4 columns',\n};\nconsole.log('Tailwind responsive class pattern:');\nObject.entries(tailwindResponsive).forEach(([cls, desc]) =>\n  console.log(` ${cls} → ${desc}`));", language: "javascript", output: "Tailwind responsive class pattern:\n grid-cols-1 → Default: 1 column (mobile)\n sm:grid-cols-2 → sm (640px+): 2 columns\n md:grid-cols-3 → md (768px+): 3 columns\n lg:grid-cols-4 → lg (1024px+): 4 columns" }
  ],
  exercises: [
    { level: 1, title: "Breakpoint classifier", problem: "Write `breakpoint(width)` that returns 'mobile' for <768, 'tablet' for 768–1023, 'desktop' for 1024+.", hints: ["Use if/else if with comparisons."], solution: "function breakpoint(w){return w<768?'mobile':w<1024?'tablet':'desktop';}\n[320,768,1024,1440].forEach(w=>console.log(w,breakpoint(w)));" },
    { level: 2, title: "clamp simulator", problem: "Write `clamp(min, val, max)` returning val clamped between min and max.", hints: ["Math.min(max, Math.max(min, val))"], solution: "const clamp=(min,val,max)=>Math.min(max,Math.max(min,val));\nconsole.log(clamp(16,12,24),clamp(16,20,24),clamp(16,30,24));" },
    { level: 3, title: "Responsive column count", problem: "Write `colCount(containerWidth, minColWidth)` returning how many equal columns fit given the container and minimum column width.", hints: ["Math.floor(containerWidth / minColWidth)"], solution: "const colCount=(w,min)=>Math.floor(w/min)||1;\nconsole.log(colCount(1200,280),colCount(768,280),colCount(320,280));" }
  ],
  interview: [
    { q: "What is mobile-first design?", a: "Writing CSS starting with mobile as the base (no media query), then adding min-width media queries to enhance the layout for larger screens. Ensures mobile gets the essential, unoverridden styles, reduces CSS overrides, and aligns with Google's mobile-first indexing." },
    { q: "What is the viewport meta tag?", a: "<meta name='viewport' content='width=device-width, initial-scale=1'> tells the browser to set the viewport width to the device's actual screen width and not zoom out. Without it, mobile browsers render pages at ~980px and scale them down, breaking responsive layouts." },
    { q: "What is clamp() and when would you use it?", a: "clamp(min, preferred, max) returns preferred if it's between min and max, otherwise the limit. Used for fluid typography: `font-size: clamp(1rem, 2.5vw, 2rem)` scales smoothly between 16px (mobile) and 32px (desktop) without breakpoints." }
  ],
  realWorld: [
    { company: "Google", text: "Google switched to mobile-first indexing in 2019 — it crawls and ranks pages based on mobile content and performance. Responsive design is now a direct SEO factor." },
    { company: "Bootstrap / Tailwind", text: "Both frameworks are built on mobile-first responsive breakpoints. Understanding media queries and the mobile-first mental model lets you extend or customize these frameworks effectively." }
  ],
  quiz: [
    { q: "What does min-width: 768px in a media query mean?", options: ["Screen smaller than 768px", "Screen 768px or wider", "Exactly 768px", "Mobile only"], answer: 1 },
    { q: "What is mobile-first design?", options: ["Build mobile app first, then web", "Write base CSS for mobile, enhance for larger screens", "Only support mobile devices", "Disable desktop features"], answer: 1 },
    { q: "What does clamp(1rem, 2vw, 2rem) do?", options: ["Sets fixed font-size of 1rem", "Creates a fluid value between 1rem and 2rem", "Clamps viewport width", "Removes responsive behavior"], answer: 1 },
    { q: "What is the viewport meta tag for?", options: ["SEO optimization", "Tells browser to match device screen width", "Enables JavaScript", "Sets page language"], answer: 1 },
    { q: "Container queries differ from media queries because they:", options: ["Only work on desktop", "Query the parent container size, not the viewport", "Require JavaScript", "Only support width"], answer: 1 }
  ]
});

// MODULE 34 — Advanced CSS
window.CSFA_RAW_TOPICS.push({
  id: 'm34-animations', module: 34, title: 'CSS Animations, Transitions & Variables',
  tagline: 'Micro-interactions, keyframe animations, CSS custom properties, and glassmorphism.',
  readMinutes: 6,
  intro: {
    whatItIs: "CSS transitions animate property changes smoothly over time. CSS animations (@keyframes) create multi-step, looping animations without JavaScript. CSS custom properties (variables) enable dynamic, themeable design systems. Modern effects like glassmorphism use backdrop-filter.",
    whyItMatters: "Animations improve perceived performance and user experience. CSS variables enable dark mode, theming, and runtime customization. Understanding which animations are GPU-accelerated (transform, opacity) vs. CPU-expensive (width, color) prevents jank.",
    whereUsed: "Loading spinners, hover effects, skeleton screens, slide-in modals, dark/light mode themes, page transitions, micro-interactions — animation is everywhere in modern UI.",
    commonMistakes: "Animating layout-triggering properties (width, height, top, left) instead of transform: translate(). Use transform and opacity for smooth GPU-accelerated animations."
  },
  visual: { caption: "@keyframes: 0% → 50% → 100% states. transition: property duration easing delay", type: "animation-flow" },
  examples: [
    { difficulty: "very-easy", title: "CSS transition", explanation: "Transitions animate property changes triggered by state changes like :hover.", code: "const transition = `.button {\n  background: #7C3AED;\n  transition: background 0.3s ease, transform 0.2s ease;\n}\n.button:hover {\n  background: #6D28D9;\n  transform: translateY(-2px);\n}`;\nconsole.log('Button hover transition:');\nconsole.log(transition);", language: "javascript", output: "Button hover transition:\n.button {\n  background: #7C3AED;\n  transition: background 0.3s ease, transform 0.2s ease;\n}\n.button:hover {\n  background: #6D28D9;\n  transform: translateY(-2px);\n}" },
    { difficulty: "easy", title: "CSS variables (custom properties)", explanation: "CSS variables (--name: value) enable dynamic, cascading design tokens.", code: "const cssVars = `:root {\n  --color-primary: #7C3AED;\n  --color-bg: #0F0F1A;\n  --radius: 12px;\n  --shadow: 0 4px 24px rgba(0,0,0,0.4);\n}\n\n.card {\n  background: var(--color-bg);\n  border-radius: var(--radius);\n  box-shadow: var(--shadow);\n  color: var(--color-primary);\n}`;\nconsole.log('CSS variables in use:', cssVars.includes('var(--') ? 'yes' : 'no');", language: "javascript", output: "CSS variables in use: yes" },
    { difficulty: "medium", title: "@keyframes animation", explanation: "Define multi-step animations with @keyframes, apply with animation property.", code: "const spinnerCSS = `@keyframes spin {\n  from { transform: rotate(0deg); }\n  to   { transform: rotate(360deg); }\n}\n\n.spinner {\n  animation: spin 1s linear infinite;\n  /* name | duration | timing | iteration */\n}\n\n@keyframes pulse {\n  0%, 100% { opacity: 1; }\n  50%       { opacity: 0.5; }\n}`;\nconsole.log('Keyframe animation defined');\nconsole.log('GPU-accelerated: only transform and opacity used');", language: "javascript", output: "Keyframe animation defined\nGPU-accelerated: only transform and opacity used" },
    { difficulty: "medium-plus", title: "Dark mode with CSS variables", explanation: "CSS variables + prefers-color-scheme = clean dark mode toggle.", code: "const darkMode = `:root {\n  --bg: #ffffff;\n  --text: #1a1a2e;\n}\n\n@media (prefers-color-scheme: dark) {\n  :root {\n    --bg: #0f0f1a;\n    --text: #e2e8f0;\n  }\n}\n\n/* Or: toggle via class */\n.dark {\n  --bg: #0f0f1a;\n  --text: #e2e8f0;\n}`;\nconsole.log('Dark mode approach:', darkMode.includes('prefers-color-scheme') ? 'system-aware' : 'class-only');", language: "javascript", output: "Dark mode approach: system-aware" },
    { difficulty: "hard", title: "Glassmorphism effect", explanation: "Glassmorphism uses backdrop-filter for frosted glass UI — a popular modern design trend.", code: "const glassMorphism = `.glass {\n  background: rgba(255, 255, 255, 0.1);\n  backdrop-filter: blur(16px) saturate(180%);\n  -webkit-backdrop-filter: blur(16px) saturate(180%);\n  border: 1px solid rgba(255, 255, 255, 0.15);\n  border-radius: 16px;\n  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);\n}`;\nconsole.log('Glassmorphism CSS:', glassMorphism.includes('backdrop-filter') ? '✓' : '✗');", language: "javascript", output: "Glassmorphism CSS: ✓" },
    { difficulty: "real-world", title: "Animation performance: will-change", explanation: "will-change hints to the browser to create a GPU layer for an element before animating.", code: "const performantAnim = `.modal {\n  will-change: transform, opacity; /* create GPU layer in advance */\n}\n.modal.entering {\n  animation: slideIn 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);\n}\n@keyframes slideIn {\n  from { transform: translateY(20px); opacity: 0; }\n  to   { transform: translateY(0);    opacity: 1; }\n}`;\nconsole.log('Performance tip: only use will-change when needed');\nconsole.log('Too many will-change elements wastes GPU memory');", language: "javascript", output: "Performance tip: only use will-change when needed\nToo many will-change elements wastes GPU memory" }
  ],
  exercises: [
    { level: 1, title: "CSS variable generator", problem: "Write `cssVar(name, value)` returning a CSS custom property declaration string like '--color-primary: #7C3AED;'.", hints: ["Template literal: `--${name}: ${value};`"], solution: "const cssVar=(n,v)=>`--${n}: ${v};`;\nconsole.log(cssVar('color-primary','#7C3AED'));" },
    { level: 2, title: "Animation duration validator", problem: "Write `validDuration(val)` returning true if val is a valid CSS duration string (ends with 'ms' or 's').", hints: ["endsWith('ms') || endsWith('s')"], solution: "const validDuration=v=>v.endsWith('ms')||v.endsWith('s');\nconsole.log(validDuration('300ms'),validDuration('0.5s'),validDuration('300'));" },
    { level: 3, title: "Theme generator", problem: "Write `generateTheme(primary)` that takes a hex color and returns an object with '--color-primary': primary, '--color-bg': '#0f0f1a', '--color-text': '#e2e8f0'.", hints: ["Return object with 3 CSS variable keys."], solution: "const generateTheme=primary=>({'--color-primary':primary,'--color-bg':'#0f0f1a','--color-text':'#e2e8f0'});\nconsole.log(JSON.stringify(generateTheme('#7C3AED')));" }
  ],
  interview: [
    { q: "Why should you animate transform instead of left/top?", a: "Animating top/left/width triggers layout recalculation every frame (expensive). transform happens after layout in the compositing stage — it runs on the GPU without triggering reflow or repaint, giving smooth 60fps animations." },
    { q: "What are CSS custom properties (variables)?", a: "CSS custom properties are variables defined with --name: value syntax, accessed via var(--name). They're part of the cascade (can be overridden by child elements or media queries), making them ideal for themes, dark mode, and design systems. Unlike SCSS variables, they're dynamic — changeable at runtime via JavaScript." },
    { q: "What is glassmorphism?", a: "A UI design trend using frosted glass effects: semi-transparent backgrounds with backdrop-filter: blur() to blur content behind the element, combined with a light border and subtle shadow. Popular in macOS, iOS, and modern web UIs." }
  ],
  realWorld: [
    { company: "Apple", text: "macOS and iOS extensively use glassmorphism — the design language of blurred, translucent panels. Web implementations use backdrop-filter: blur() following the same visual principles." },
    { company: "Framer Motion / CSS", text: "Modern React animation libraries like Framer Motion compile to CSS transforms and opacity under the hood, deliberately avoiding layout-triggering properties for performance." }
  ],
  quiz: [
    { q: "Which CSS property creates a smooth frosted-glass blur effect?", options: ["filter: blur()", "backdrop-filter: blur()", "background: blur()", "opacity: 0.5"], answer: 1 },
    { q: "Which property should you animate for performance?", options: ["width", "height", "top", "transform"], answer: 3 },
    { q: "How do you define a CSS variable?", options: ["$color: red", "--color: red", "var(color): red", "@color: red"], answer: 1 },
    { q: "How do you use a CSS variable?", options: ["$color-primary", "@color-primary", "var(--color-primary)", "#{color-primary}"], answer: 2 },
    { q: "What does will-change do?", options: ["Prevents element from changing", "Hints browser to create GPU layer in advance", "Forces repaint", "Disables animations"], answer: 1 }
  ]
});

// MODULE 35 — JavaScript Basics
window.CSFA_RAW_TOPICS.push({
  id: 'm35-js-syntax', module: 35, title: 'JavaScript Syntax & Core Concepts',
  tagline: 'The essential syntax of JavaScript — the language that makes the web interactive.',
  readMinutes: 5,
  intro: {
    whatItIs: "JavaScript is a dynamically typed, interpreted (JIT-compiled) language that runs in browsers and Node.js. Key syntax: variables (let/const), functions, objects, arrays, template literals, optional chaining (?.), nullish coalescing (??), and destructuring. ES6+ (ES2015+) introduced many modern features now used everywhere.",
    whyItMatters: "JavaScript is the only language that runs natively in browsers. Understanding modern JS syntax (optional chaining, nullish coalescing, destructuring) makes code significantly more concise and bug-resistant.",
    whereUsed: "Frontend (browsers), backend (Node.js), mobile (React Native), desktop (Electron), serverless (Edge functions) — JavaScript is the most deployed programming language in the world.",
    commonMistakes: "Using optional chaining (?.) when you actually want to throw on null — it silently returns undefined instead. Only use ?. when null/undefined is a valid, expected state."
  },
  visual: { caption: "Modern JS: let/const → template literals → destructuring → optional chaining → nullish coalesc.", type: "js-syntax" },
  examples: [
    { difficulty: "very-easy", title: "Template literals", explanation: "Template literals (backticks) allow embedded expressions and multiline strings.", code: "const name = 'Alice';\nconst age = 30;\nconsole.log(`Hello, ${name}! You are ${age} years old.`);\nconsole.log(`${2 + 2} is four`);\nconsole.log(`Multi\nline\nstring`);", language: "javascript", output: "Hello, Alice! You are 30 years old.\n4 is four\nMulti\nline\nstring" },
    { difficulty: "easy", title: "Optional chaining (?.) and nullish coalescing (??)", explanation: "?. safely accesses nested properties; ?? provides a fallback for null/undefined.", code: "const user = { profile: { name: 'Bob' } };\nconsole.log(user?.profile?.name);     // 'Bob'\nconsole.log(user?.settings?.theme);   // undefined (no crash)\nconsole.log(user?.settings?.theme ?? 'dark'); // 'dark' (default)", language: "javascript", output: "Bob\nundefined\ndark" },
    { difficulty: "medium", title: "Destructuring", explanation: "Destructuring extracts values from arrays or objects into variables.", code: "// Object destructuring\nconst { name, age, role = 'user' } = { name: 'Alice', age: 30 };\nconsole.log(name, age, role);\n\n// Array destructuring\nconst [first, second, ...rest] = [10, 20, 30, 40, 50];\nconsole.log(first, second, rest);\n\n// Rename on destruct\nconst { name: userName } = { name: 'Bob' };\nconsole.log(userName);", language: "javascript", output: "Alice 30 user\n10 20 [ 30, 40, 50 ]\nBob" },
    { difficulty: "medium-plus", title: "Spread and rest operators", explanation: "Spread (...) expands iterables; rest collects remaining items.", code: "// Spread: copy and merge arrays/objects\nconst arr1 = [1,2,3];\nconst arr2 = [...arr1, 4, 5];\nconsole.log('Spread array:', arr2);\n\nconst base = { a: 1, b: 2 };\nconst extended = { ...base, c: 3 };\nconsole.log('Spread object:', extended);\n\n// Rest: collect remaining function args\nfunction sum(first, ...others) {\n  return first + others.reduce((a,b) => a+b, 0);\n}\nconsole.log('Sum:', sum(1, 2, 3, 4));", language: "javascript", output: "Spread array: [ 1, 2, 3, 4, 5 ]\nSpread object: { a: 1, b: 2, c: 3 }\nSum: 10" },
    { difficulty: "hard", title: "Short-circuit patterns", explanation: "&&, ||, ?? are used for conditional rendering and defaults in modern JS.", code: "const isLoggedIn = true;\nconst username = '';\nconst theme = null;\n\n// && for conditional rendering\nconsole.log(isLoggedIn && 'Welcome back!');\n// || for fallback (falsy check)\nconsole.log(username || 'Anonymous');\n// ?? for null/undefined only\nconsole.log(username ?? 'Anonymous'); // '' is not null, so keeps ''\nconsole.log(theme ?? 'dark');", language: "javascript", output: "Welcome back!\nAnonymous\n\ndark" },
    { difficulty: "real-world", title: "Modern API response handling", explanation: "Combining optional chaining, nullish coalescing, and destructuring for clean API handling.", code: "function processUser(response) {\n  const { data: user, status } = response ?? {};\n  const name = user?.profile?.displayName ?? user?.email ?? 'Unknown';\n  const avatar = user?.avatarUrl ?? `https://api.dicebear.com/avatar/${name}`;\n  return { name, avatar, status: status ?? 200 };\n}\nconsole.log(processUser({ data: { profile: { displayName: 'Alice' }, email: 'a@b.com' }, status: 200 }));\nconsole.log(processUser(null));", language: "javascript", output: "{ name: 'Alice', avatar: 'https://api.dicebear.com/avatar/Alice', status: 200 }\n{ name: 'Unknown', avatar: 'https://api.dicebear.com/avatar/Unknown', status: 200 }" }
  ],
  exercises: [
    { level: 1, title: "Template literal builder", problem: "Write `greeting(name, hour)` returning 'Good morning, Alice!' for hour<12, 'Good afternoon, Alice!' for 12-17, 'Good evening, Alice!' otherwise.", hints: ["Use template literal with conditional expression."], solution: "function greeting(n,h){const t=h<12?'morning':h<18?'afternoon':'evening';return `Good ${t}, ${n}!`;}\nconsole.log(greeting('Alice',9),greeting('Bob',14),greeting('Eve',20));" },
    { level: 2, title: "Safe property access", problem: "Write `safeGet(obj, ...keys)` that safely traverses nested properties like safeGet(user, 'profile', 'name') returning the value or undefined.", hints: ["Use reduce with ?. or try/catch."], solution: "function safeGet(obj,...keys){return keys.reduce((o,k)=>o?.[k],obj);}\nconsole.log(safeGet({a:{b:{c:42}}},'a','b','c')); // 42\nconsole.log(safeGet({a:{}},'a','b','c')); // undefined" },
    { level: 3, title: "Config merger", problem: "Write `mergeConfig(defaults, overrides)` that merges two config objects using spread, with overrides taking priority.", hints: ["Return { ...defaults, ...overrides }"], solution: "const mergeConfig=(d,o)=>({...d,...o});\nconsole.log(mergeConfig({theme:'light',lang:'en'},{theme:'dark',debug:true}));" }
  ],
  interview: [
    { q: "What is the difference between || and ??", a: "|| returns the right side when the left is falsy (0, '', false, null, undefined). ?? (nullish coalescing) only returns the right side when the left is null or undefined — not for 0 or empty string. Use ?? when 0 or '' are valid values." },
    { q: "What does optional chaining (?.) do?", a: "?. short-circuits and returns undefined if the value before it is null or undefined, instead of throwing a TypeError. user?.profile?.name returns undefined if user or profile is null/undefined. Eliminates verbose `user && user.profile && user.profile.name` patterns." },
    { q: "What is destructuring?", a: "Destructuring extracts values from arrays or objects into variables in a single expression. Object: `const {name, age} = user`. Array: `const [first, ...rest] = items`. Supports defaults and renaming: `const {name: userName = 'Guest'} = user`." }
  ],
  realWorld: [
    { company: "React", text: "React components use destructuring constantly: `function Button({ label, onClick, disabled = false })` — the JSX function signature destructures props directly, with default values. Modern JS features are core to React development." },
    { company: "Node.js ecosystem", text: "The entire npm ecosystem relies on modern JS features. Tools like Next.js, Express, and Fastify use optional chaining, nullish coalescing, and spread operator throughout their codebases." }
  ],
  quiz: [
    { q: "What does user?.settings?.theme return if settings is undefined?", options: ["TypeError", "null", "undefined", "false"], answer: 2 },
    { q: "What does '' ?? 'default' return?", options: ["'default'", "''", "null", "false"], answer: 1 },
    { q: "What is the result of const [a,...rest] = [1,2,3]?", options: ["a=1,rest=[2,3]", "a=[1,2,3],rest=[]", "a=1,rest=2", "Error"], answer: 0 },
    { q: "What does spread operator do to arrays?", options: ["Sorts them", "Expands elements into a new array or function arguments", "Reverses them", "Filters them"], answer: 1 },
    { q: "What does `0 || 'fallback'` return?", options: ["0", "false", "'fallback'", "null"], answer: 2 }
  ]
});
