/* Module 38 — Authentication & Security */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }
window.CSFA_RAW_TOPICS.push({
  id: 'm38-jwt-auth', module: 38, title: 'Authentication — JWT, Sessions & OAuth',
  tagline: 'Proving who you are — stateless tokens, cookies, sessions, and OAuth flows.',
  readMinutes: 7,
  intro: {
    whatItIs: "Authentication verifies identity; authorization checks permissions. JWT (JSON Web Token) is a stateless, self-contained token signed with a secret. Sessions use server-side storage with a cookie reference. OAuth 2.0 / OpenID Connect delegates authentication to providers like Google, GitHub, or Auth0.",
    whyItMatters: "Every app with users needs auth. Security mistakes here are catastrophic — token leaks, CSRF, session fixation, and insecure password storage are in every OWASP Top 10. Understanding the tradeoffs of JWT vs sessions prevents real vulnerabilities.",
    whereUsed: "Every user-facing application. JWTs in REST APIs and microservices. Sessions in monolithic web apps. OAuth for 'Login with Google'. Next-Auth, Passport.js, Auth0, Clerk for implementation.",
    commonMistakes: "Storing JWTs in localStorage — XSS attacks can steal them. Store access tokens in memory and refresh tokens in httpOnly cookies. Never store passwords in plaintext — always use bcrypt or Argon2."
  },
  visual: { caption: "Login → server issues JWT → client sends JWT in Authorization header → server verifies", type: "jwt-flow" },
  examples: [
    { difficulty: "very-easy", title: "JWT structure", explanation: "A JWT has 3 parts: header.payload.signature — all base64url-encoded.", code: "const jwtParts = {\n  header: { alg: 'HS256', typ: 'JWT' },\n  payload: { sub: '1234567890', name: 'Alice', iat: 1516239022, exp: 1516242622 },\n  signature: 'HMACSHA256(base64(header)+\".\"+base64(payload), secret)'\n};\nconsole.log('Header:', JSON.stringify(jwtParts.header));\nconsole.log('Payload:', JSON.stringify(jwtParts.payload));\nconsole.log('Expires in:', (jwtParts.payload.exp - jwtParts.payload.iat) / 60, 'minutes');", language: "javascript", output: "Header: {\"alg\":\"HS256\",\"typ\":\"JWT\"}\nPayload: {\"sub\":\"1234567890\",\"name\":\"Alice\",\"iat\":1516239022,\"exp\":1516242622}\nExpires in: 60 minutes" },
    { difficulty: "easy", title: "JWT vs Session comparison", explanation: "JWTs are stateless (no DB lookup needed). Sessions are stateful (server stores session data).", code: "const comparison = [\n  { aspect: 'Storage',      jwt: 'Client-side (memory/cookie)', session: 'Server-side (DB/Redis)' },\n  { aspect: 'Scalability', jwt: 'Stateless — any server',       session: 'Sticky sessions or shared store' },\n  { aspect: 'Revocation',  jwt: 'Hard (must wait for expiry)',  session: 'Easy (delete from store)' },\n  { aspect: 'Size',        jwt: 'Larger (payload in token)',    session: 'Smaller (only session ID)' },\n];\ncomparison.forEach(c => console.log(`${c.aspect.padEnd(12)}: JWT=${c.jwt.padEnd(30)} | Session=${c.session}`));", language: "javascript", output: "Storage     : JWT=Client-side (memory/cookie)   | Session=Server-side (DB/Redis)\nScalability : JWT=Stateless — any server          | Session=Sticky sessions or shared store\nRevocation  : JWT=Hard (must wait for expiry)      | Session=Easy (delete from store)\nSize        : JWT=Larger (payload in token)        | Session=Smaller (only session ID)" },
    { difficulty: "medium", title: "Password hashing concept", explanation: "Never store plaintext passwords. bcrypt adds a salt and is intentionally slow to thwart brute force.", code: "// Conceptual — actual bcrypt requires the library\nasync function hashPassword(password) {\n  // bcrypt.hash(password, 12) — 12 rounds\n  const simulatedHash = `$2b$12$${password.length}chars.salt.${Math.random().toString(36).slice(2)}`;\n  return simulatedHash;\n}\nasync function verifyPassword(password, hash) {\n  // bcrypt.compare(password, hash)\n  return hash.includes(`${password.length}chars`); // simplified\n}\nconst hash = await hashPassword('secret123');\nconsole.log('Hash:', hash.slice(0, 30) + '...');\nconsole.log('Verify:', await verifyPassword('secret123', hash));", language: "javascript", output: "Hash: $2b$12$9chars.salt.k7m...\nVerify: true" },
    { difficulty: "medium-plus", title: "JWT verification simulation", explanation: "Server verifies JWT signature and expiry before trusting the payload.", code: "function verifyJWT(token, secret) {\n  const [header, payload, sig] = token.split('.');\n  // In real JWT: signature is HMAC of header.payload with secret\n  const expectedSig = `HMAC(${header}.${payload}, ${secret})`;\n  if (sig !== expectedSig) throw new Error('Invalid signature');\n  const data = JSON.parse(atob(payload));\n  if (data.exp < Date.now()/1000) throw new Error('Token expired');\n  return data;\n}\ntry {\n  const token = `eyJhbGciOiJIUzI1NiJ9.${btoa(JSON.stringify({sub:'123',exp:9999999999}))}.HMAC(eyJhbGciOiJIUzI1NiJ9.${btoa(JSON.stringify({sub:'123',exp:9999999999}))}, secret)`;\n  const data = verifyJWT(token, 'secret');\n  console.log('Valid token, sub:', data.sub);\n} catch(e) { console.log('Auth error:', e.message); }", language: "javascript", output: "Valid token, sub: 123" },
    { difficulty: "hard", title: "OAuth 2.0 authorization code flow", explanation: "OAuth: redirect user to provider → get auth code → exchange for access token.", code: "const oauthFlow = [\n  '1. User clicks \"Login with GitHub\"',\n  '2. Browser → GitHub /oauth/authorize?client_id=...&redirect_uri=...&scope=user',\n  '3. User grants permission on GitHub',\n  '4. GitHub → redirect_uri?code=AUTHCODE',\n  '5. Server exchanges: POST /oauth/token {code, client_secret} → {access_token}',\n  '6. Server fetches: GET /user with access_token → user profile',\n  '7. Server creates session/JWT for user',\n];\noauthFlow.forEach(s => console.log(s));", language: "javascript", output: "1. User clicks \"Login with GitHub\"\n2. Browser → GitHub /oauth/authorize?client_id=...&redirect_uri=...&scope=user\n3. User grants permission on GitHub\n4. GitHub → redirect_uri?code=AUTHCODE\n5. Server exchanges: POST /oauth/token {code, client_secret} → {access_token}\n6. Server fetches: GET /user with access_token → user profile\n7. Server creates session/JWT for user" },
    { difficulty: "real-world", title: "Refresh token rotation", explanation: "Short-lived access tokens + long-lived refresh tokens = security + usability.", code: "const tokenStrategy = {\n  accessToken:  { storage: 'memory (JS variable)', ttl: '15 minutes', purpose: 'API calls' },\n  refreshToken: { storage: 'httpOnly cookie',       ttl: '30 days',    purpose: 'Get new access token' },\n};\nconsole.log('Access token:', JSON.stringify(tokenStrategy.accessToken));\nconsole.log('Refresh token:', JSON.stringify(tokenStrategy.refreshToken));\nconsole.log('\\nWhy: httpOnly cookie = XSS-safe. Short access TTL = minimal breach window.');\nconsole.log('Rotation: each refresh issues a new refresh token, invalidating the old one.');", language: "javascript", output: "Access token: {\"storage\":\"memory (JS variable)\",\"ttl\":\"15 minutes\",\"purpose\":\"API calls\"}\nRefresh token: {\"storage\":\"httpOnly cookie\",\"ttl\":\"30 days\",\"purpose\":\"Get new access token\"}\n\nWhy: httpOnly cookie = XSS-safe. Short access TTL = minimal breach window.\nRotation: each refresh issues a new refresh token, invalidating the old one." }
  ],
  exercises: [
    { level: 1, title: "JWT parser", problem: "Write `parseJWT(token)` that splits a JWT string on '.' and base64-decodes (atob) the middle part (payload) into a JS object.", hints: ["token.split('.')[1] → atob() → JSON.parse()"], solution: "function parseJWT(t){try{return JSON.parse(atob(t.split('.')[1]));}catch{return null;}}\nconsole.log(parseJWT(`h.${btoa(JSON.stringify({sub:'123',name:'Alice'}))}.s`));" },
    { level: 2, title: "Token expiry check", problem: "Write `isExpired(payload)` where payload has an `exp` field (Unix timestamp seconds). Return true if token is expired.", hints: ["Compare payload.exp < Date.now()/1000."], solution: "const isExpired=p=>p.exp<Date.now()/1000;\nconsole.log(isExpired({exp:1000000})); // expired long ago\nconsole.log(isExpired({exp:9999999999})); // future date" },
    { level: 3, title: "Auth middleware", problem: "Write `authMiddleware(req, next, onError)` that checks req.headers.authorization for a 'Bearer ' token, calls onError('401') if missing, or next() if present.", hints: ["Check if authorization starts with 'Bearer '."], solution: "function authMiddleware(req,next,onError){const auth=req.headers?.authorization;if(!auth?.startsWith('Bearer '))return onError('401 Unauthorized');next();}\nauthMiddleware({headers:{authorization:'Bearer tok'}},()=>console.log('OK'),console.log);\nauthMiddleware({headers:{}},()=>console.log('OK'),console.log);" }
  ],
  interview: [
    { q: "Where should you store JWTs in the browser?", a: "Access tokens in memory (JS variable). Refresh tokens in httpOnly cookies (inaccessible to JS, preventing XSS theft). localStorage is vulnerable to XSS — any injected script can steal tokens. httpOnly cookies are immune to JS access." },
    { q: "What is the difference between authentication and authorization?", a: "Authentication: verifying WHO you are (login, JWT verification). Authorization: verifying WHAT you're allowed to do (role check, permission check). A user can be authenticated but not authorized to access a specific resource." },
    { q: "What is CSRF and how do you prevent it?", a: "Cross-Site Request Forgery tricks a logged-in user's browser into making unwanted requests to your API. Prevention: SameSite=Strict cookies, CSRF tokens (server issues token, client sends it in header), or checking Origin/Referer headers." }
  ],
  realWorld: [
    { company: "Auth0 / Clerk", text: "Auth0 and Clerk implement the full OAuth/OIDC flow, refresh token rotation, device fingerprinting, and anomaly detection — allowing teams to outsource auth complexity to specialists rather than building it insecurely from scratch." },
    { company: "GitHub", text: "GitHub uses OAuth 2.0 for GitHub Apps and personal access tokens. Their fine-grained PATs implement the principle of least privilege — tokens only have the exact permissions requested, limiting breach impact." }
  ],
  quiz: [
    { q: "Where should access tokens be stored for XSS safety?", options: ["localStorage", "sessionStorage", "In-memory (JS variable)", "IndexedDB"], answer: 2 },
    { q: "What are the three parts of a JWT?", options: ["Header, Body, Footer", "Header, Payload, Signature", "ID, Data, Hash", "Type, Claims, Key"], answer: 1 },
    { q: "Why use bcrypt for passwords?", options: ["It's fast", "Intentionally slow + adds salt, thwarting brute force", "It compresses passwords", "It encrypts reversibly"], answer: 1 },
    { q: "What is the OAuth authorization code flow for?", options: ["Server-to-server auth", "Delegating authentication to a trusted provider", "Encrypting API responses", "Rate limiting"], answer: 1 },
    { q: "What does httpOnly cookie prevent?", options: ["CSRF attacks", "Network interception", "JavaScript from reading the cookie (prevents XSS theft)", "Cookie expiry"], answer: 2 }
  ]
});
