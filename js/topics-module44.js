/* Modules 54–65: Algorithms, AI/LLM, Performance, Career topics */
if (!window.CSFA_RAW_TOPICS) { window.CSFA_RAW_TOPICS = []; }

// MODULE 54 — Algorithms & Data Structures Review
window.CSFA_RAW_TOPICS.push({
  id: 'm54-dsa-review', module: 54, title: 'Big-O & Algorithm Complexity',
  tagline: 'Measure code efficiency — time and space complexity analysis for interviews.',
  readMinutes: 7,
  intro: {
    whatItIs: "Big-O notation describes how an algorithm's runtime or space grows as input size n grows. Common complexities: O(1) constant, O(log n) logarithmic, O(n) linear, O(n log n) linearithmic, O(n²) quadratic, O(2^n) exponential. Time complexity measures operations; space complexity measures memory.",
    whyItMatters: "Algorithm complexity determines whether your code works for 100 users or 10 million. A nested loop is O(n²) — fine for n=100, catastrophic for n=100,000. Every technical interview evaluates Big-O understanding.",
    whereUsed: "Technical interviews (FAANG, top startups), performance optimization, choosing data structures, database query analysis, system design.",
    commonMistakes: "Only optimizing for time, ignoring space complexity. Also: premature optimization. Measure first — the bottleneck is rarely where you expect."
  },
  visual: { caption: "O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2^n) — growth rates as n→∞", type: "big-o-chart" },
  examples: [
    { difficulty: "very-easy", title: "Common complexities", explanation: "Examples of each complexity class with real algorithm mappings.", code: "const complexities = [\n  { O: 'O(1)',       example: 'Array access arr[i], HashMap get/set' },\n  { O: 'O(log n)',  example: 'Binary search, BST operations' },\n  { O: 'O(n)',      example: 'Linear scan, sum array, find max' },\n  { O: 'O(n log n)',example: 'Merge sort, heap sort, most efficient sorts' },\n  { O: 'O(n²)',     example: 'Bubble/insertion sort, nested loops' },\n  { O: 'O(2^n)',    example: 'Subsets of a set, naive recursive Fibonacci' },\n];\ncomplexities.forEach(c => console.log(`${c.O.padEnd(10)}: ${c.example}`));", language: "javascript", output: "O(1)      : Array access arr[i], HashMap get/set\nO(log n)  : Binary search, BST operations\nO(n)      : Linear scan, sum array, find max\nO(n log n): Merge sort, heap sort, most efficient sorts\nO(n²)     : Bubble/insertion sort, nested loops\nO(2^n)    : Subsets of a set, naive recursive Fibonacci" },
    { difficulty: "easy", title: "Nested loops = O(n²)", explanation: "Each loop variable multiplies complexity — two loops over n = O(n²).", code: "function findPairs(arr, target) {\n  const pairs = [];\n  for (let i = 0; i < arr.length; i++) {       // O(n)\n    for (let j = i+1; j < arr.length; j++) {   // O(n)\n      if (arr[i] + arr[j] === target) pairs.push([arr[i], arr[j]]);\n    }\n  }\n  return pairs; // Total: O(n²)\n}\nconsole.log(findPairs([1,2,3,4,5], 6));\n// O(n) solution: use a Set — O(n) time, O(n) space", language: "javascript", output: "[ [ 1, 5 ], [ 2, 4 ] ]" },
    { difficulty: "medium", title: "Binary search O(log n)", explanation: "Binary search halves the search space each step — O(log n).", code: "function binarySearch(arr, target) {\n  let lo = 0, hi = arr.length - 1, steps = 0;\n  while (lo <= hi) {\n    steps++;\n    const mid = Math.floor((lo + hi) / 2);\n    if (arr[mid] === target) { console.log(`Found in ${steps} steps`); return mid; }\n    else if (arr[mid] < target) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return -1;\n}\nconst sorted = Array.from({length:1000},(_,i)=>i);\nbinarySearch(sorted, 777); // max 10 steps for 1000 elements", language: "javascript", output: "Found in 4 steps" },
    { difficulty: "medium-plus", title: "Space complexity", explanation: "Space complexity counts extra memory allocated proportional to input size.", code: "function reverseArray(arr) {\n  const reversed = [];       // O(n) space — new array\n  for (let i = arr.length-1; i >= 0; i--)\n    reversed.push(arr[i]);\n  return reversed;\n}\nfunction reverseInPlace(arr) {\n  let lo=0, hi=arr.length-1; // O(1) space — no extra array\n  while (lo < hi) { [arr[lo],arr[hi]] = [arr[hi],arr[lo]]; lo++; hi--; }\n  return arr;\n}\nconst arr = [1,2,3,4,5];\nconsole.log('O(n) space:', reverseArray([...arr]));\nconsole.log('O(1) space:', reverseInPlace([...arr]));", language: "javascript", output: "O(n) space: [ 5, 4, 3, 2, 1 ]\nO(1) space: [ 5, 4, 3, 2, 1 ]" },
    { difficulty: "hard", title: "Two pointers: O(n) pair sum", explanation: "Two pointers solve pair-sum in O(n) — faster than O(n²) nested loops.", code: "function twoSum(sortedArr, target) {\n  let lo = 0, hi = sortedArr.length - 1;\n  while (lo < hi) {\n    const sum = sortedArr[lo] + sortedArr[hi];\n    if (sum === target) return [sortedArr[lo], sortedArr[hi]];\n    else if (sum < target) lo++;\n    else hi--;\n  }\n  return null;\n}\nconsole.log(twoSum([1,2,3,4,5,6,7], 9));\nconsole.log('O(n) vs O(n²) for nested loop approach');", language: "javascript", output: "[ 2, 7 ]\nO(n) vs O(n²) for nested loop approach" },
    { difficulty: "real-world", title: "Sliding window: max subarray sum", explanation: "Sliding window technique solves contiguous subarray problems in O(n).", code: "function maxSubarraySum(arr, k) {\n  let windowSum = arr.slice(0,k).reduce((a,b)=>a+b,0);\n  let maxSum = windowSum;\n  for (let i=k; i<arr.length; i++) {\n    windowSum += arr[i] - arr[i-k]; // O(1) per step\n    maxSum = Math.max(maxSum, windowSum);\n  }\n  return maxSum;\n}\nconsole.log(maxSubarraySum([2,1,5,1,3,2], 3)); // max of 3 consecutive\nconsole.log('Time: O(n), Space: O(1)');", language: "javascript", output: "9\nTime: O(n), Space: O(1)" }
  ],
  exercises: [
    { level: 1, title: "Complexity identifier", problem: "Write `identifyComplexity(loops)` returning the Big-O based on number of nested loops over n: 0→O(1), 1→O(n), 2→O(n²), 3→O(n³).", hints: ["Map 0→'O(1)', 1→'O(n)', etc."], solution: "const identifyComplexity=l=>['O(1)','O(n)','O(n²)','O(n³)'][l]||'O(nˡ)';\n[0,1,2,3].forEach(l=>console.log(l,'loops →',identifyComplexity(l)));" },
    { level: 2, title: "Binary search", problem: "Write `binarySearch(sorted, target)` returning the index of target in a sorted array, or -1 if not found.", hints: ["lo=0, hi=len-1. mid=Math.floor((lo+hi)/2). Compare and narrow."], solution: "function binarySearch(arr,t){let lo=0,hi=arr.length-1;while(lo<=hi){const m=(lo+hi)>>1;if(arr[m]===t)return m;arr[m]<t?lo=m+1:hi=m-1;}return-1;}\nconsole.log(binarySearch([1,3,5,7,9],7));" },
    { level: 3, title: "Two sum O(n)", problem: "Write `twoSum(arr, target)` returning indices [i,j] of two numbers summing to target using a HashMap for O(n) time.", hints: ["Map: value→index. For each num, check if (target-num) is in map."], solution: "function twoSum(arr,t){const m=new Map();for(let i=0;i<arr.length;i++){const need=t-arr[i];if(m.has(need))return[m.get(need),i];m.set(arr[i],i);}return[];}\nconsole.log(twoSum([2,7,11,15],9));" }
  ],
  interview: [
    { q: "What is the difference between time and space complexity?", a: "Time complexity counts the number of operations as a function of input size n. Space complexity counts the extra memory allocated. Both use Big-O notation. An algorithm can trade time for space: memoization uses O(n) space to reduce time from O(2^n) to O(n)." },
    { q: "Why is O(n log n) the best possible for comparison-based sorting?", a: "Information theory lower bound: sorting n items requires at least log₂(n!) comparisons to distinguish all possible orderings. By Stirling's approximation, log₂(n!) ≈ n log n. Merge sort and heap sort achieve this bound. O(n) sorting is possible without comparisons (counting sort, radix sort) but requires constrained input." },
    { q: "What is amortized complexity?", a: "Average complexity per operation over a sequence of operations. JavaScript Array.push() is O(1) amortized — usually O(1), but occasionally O(n) when the array must resize. The cost of resizing is spread (amortized) over all push operations. Dynamic arrays double in size, making resizes exponentially rare." }
  ],
  realWorld: [
    { company: "Google/Meta/Amazon interviews", text: "FAANG technical interviews require Big-O analysis for every algorithm solution. Candidates must state time and space complexity, identify bottlenecks, and often propose O(n) or O(n log n) improvements to O(n²) brute force solutions." },
    { company: "Database Query Planner", text: "PostgreSQL's EXPLAIN ANALYZE output shows actual query plan cost — which maps directly to Big-O analysis. A sequential scan is O(n); an index scan is O(log n). Understanding complexity explains why adding an index can turn a 10-second query into a 1ms query." }
  ],
  quiz: [
    { q: "What is the time complexity of binary search?", options: ["O(n)", "O(log n)", "O(n log n)", "O(1)"], answer: 1 },
    { q: "Two nested for loops over n items =", options: ["O(n)", "O(2n)", "O(n²)", "O(log n)"], answer: 2 },
    { q: "What is O(n log n) typical of?", options: ["Hash table lookup", "Linear scan", "Efficient sorting (merge sort)", "Nested loops"], answer: 2 },
    { q: "The two-pointer technique optimizes pair-finding from O(n²) to:", options: ["O(1)", "O(log n)", "O(n)", "O(n log n)"], answer: 2 },
    { q: "Space complexity measures:", options: ["CPU usage", "Network bandwidth", "Memory allocated proportional to input size", "Execution time"], answer: 2 }
  ]
});

// MODULE 55 — Sorting & Searching
window.CSFA_RAW_TOPICS.push({
  id: 'm55-sorting', module: 55, title: 'Sorting & Searching Algorithms',
  tagline: 'From bubble sort to merge sort — how computers order and find data efficiently.',
  readMinutes: 7,
  intro: {
    whatItIs: "Sorting algorithms arrange items in order. Key algorithms: Bubble Sort O(n²), Selection Sort O(n²), Insertion Sort O(n²) — simple but slow. Merge Sort O(n log n), Quick Sort O(n log n) average — efficient. Heap Sort O(n log n) — in-place. Counting/Radix Sort O(n) — non-comparison. JavaScript's Array.sort() uses TimSort (merge + insertion).",
    whyItMatters: "Sorting is fundamental to computer science and interviews. Understanding why merge sort is O(n log n) and bubble sort is O(n²) explains algorithmic thinking. Searching (binary search) requires sorted data.",
    whereUsed: "Database ORDER BY, search result ranking, file system directory listings, event processing, priority queues, and every sorted UI display.",
    commonMistakes: "Not realizing Array.sort() converts elements to strings by default in JavaScript: [10,9,2].sort() → [10,2,9] (lexicographic). Always provide a comparator: arr.sort((a,b) => a-b) for numeric sort."
  },
  visual: { caption: "Merge Sort: divide array in half → sort each → merge. O(n log n) guaranteed.", type: "sorting" },
  examples: [
    { difficulty: "very-easy", title: "JavaScript Array.sort() gotcha", explanation: "Array.sort() lexicographic by default — always provide numeric comparator.", code: "const nums = [10, 9, 2, 100, 21];\nconsole.log('Wrong (lexicographic):', [...nums].sort().join(', '));\nconsole.log('Correct (numeric asc):', [...nums].sort((a,b) => a-b).join(', '));\nconsole.log('Correct (numeric desc):', [...nums].sort((a,b) => b-a).join(', '));\n// Sort objects\nconst users = [{name:'Bob',age:30},{name:'Alice',age:25}];\nconsole.log('By age:', users.sort((a,b)=>a.age-b.age).map(u=>u.name).join(', '));", language: "javascript", output: "Wrong (lexicographic): 10, 100, 2, 21, 9\nCorrect (numeric asc): 2, 9, 10, 21, 100\nCorrect (numeric desc): 100, 21, 10, 9, 2\nBy age: Alice, Bob" },
    { difficulty: "easy", title: "Bubble Sort", explanation: "Bubble sort: repeatedly swap adjacent elements if out of order. O(n²).", code: "function bubbleSort(arr) {\n  const a = [...arr];\n  let swaps = 0;\n  for (let i=0; i<a.length; i++)\n    for (let j=0; j<a.length-i-1; j++)\n      if (a[j] > a[j+1]) { [a[j],a[j+1]] = [a[j+1],a[j]]; swaps++; }\n  console.log(`Sorted in ${swaps} swaps`);\n  return a;\n}\nconsole.log(bubbleSort([64, 34, 25, 12, 22, 11, 90]));", language: "javascript", output: "Sorted in 15 swaps\n[\n  11, 12, 22,\n  25, 34, 64,\n  90\n]" },
    { difficulty: "medium", title: "Merge Sort", explanation: "Merge sort: divide, sort each half, merge. O(n log n) guaranteed.", code: "function mergeSort(arr) {\n  if (arr.length <= 1) return arr;\n  const mid = Math.floor(arr.length / 2);\n  const left = mergeSort(arr.slice(0, mid));\n  const right = mergeSort(arr.slice(mid));\n  return merge(left, right);\n}\nfunction merge(a, b) {\n  const result = [];\n  let i=0, j=0;\n  while (i<a.length && j<b.length)\n    result.push(a[i] <= b[j] ? a[i++] : b[j++]);\n  return [...result, ...a.slice(i), ...b.slice(j)];\n}\nconsole.log(mergeSort([38,27,43,3,9,82,10]));", language: "javascript", output: "[\n   3,  9, 10,\n  27, 38, 43,\n  82\n]" },
    { difficulty: "medium-plus", title: "Quick Sort", explanation: "Quick sort: pick pivot, partition, recurse. O(n log n) avg, O(n²) worst.", code: "function quickSort(arr) {\n  if (arr.length <= 1) return arr;\n  const pivot = arr[Math.floor(arr.length/2)];\n  const left = arr.filter(x => x < pivot);\n  const mid = arr.filter(x => x === pivot);\n  const right = arr.filter(x => x > pivot);\n  return [...quickSort(left), ...mid, ...quickSort(right)];\n}\nconsole.log(quickSort([3,6,8,10,1,2,1]));", language: "javascript", output: "[\n  1, 1, 2, 3,\n  6, 8, 10\n]" },
    { difficulty: "hard", title: "Sorting algorithm comparison", explanation: "Choosing the right algorithm depends on data characteristics.", code: "const algorithms = [\n  { name: 'Bubble Sort',    time: 'O(n²)',      space: 'O(1)',  stable: true,  use: 'Educational only' },\n  { name: 'Insertion Sort', time: 'O(n²)',      space: 'O(1)',  stable: true,  use: 'Nearly sorted, small arrays' },\n  { name: 'Merge Sort',     time: 'O(n log n)', space: 'O(n)',  stable: true,  use: 'Linked lists, stable sort needed' },\n  { name: 'Quick Sort',     time: 'O(n log n)', space: 'O(log n)', stable: false, use: 'Arrays, fastest in practice' },\n  { name: 'Heap Sort',      time: 'O(n log n)', space: 'O(1)',  stable: false, use: 'Guaranteed O(n log n), in-place' },\n];\nalgorithms.forEach(a => console.log(`${a.name.padEnd(15)}: ${a.time} | ${a.use}`));", language: "javascript", output: "Bubble Sort    : O(n²) | Educational only\nInsertion Sort : O(n²) | Nearly sorted, small arrays\nMerge Sort     : O(n log n) | Linked lists, stable sort needed\nQuick Sort     : O(n log n) | Arrays, fastest in practice\nHeap Sort      : O(n log n) | Guaranteed O(n log n), in-place" },
    { difficulty: "real-world", title: "TimSort (JavaScript's sort)", explanation: "V8's Array.sort uses TimSort — hybrid of merge sort + insertion sort.", code: "// TimSort: insertion sort for small arrays (<32), merge sort for larger\nconst INSERTION_THRESHOLD = 32;\nfunction timsortConcept(arr) {\n  if (arr.length < INSERTION_THRESHOLD) {\n    console.log(`n=${arr.length} < ${INSERTION_THRESHOLD}: using insertion sort`);\n  } else {\n    console.log(`n=${arr.length}: using merge sort with insertion for runs`);\n  }\n  return arr.sort((a,b) => a-b);\n}\nconsole.log(timsortConcept([5,2,8,1,9]));\nconsole.log(timsortConcept(Array.from({length:100},()=>Math.floor(Math.random()*100))).slice(0,5));", language: "javascript", output: "n=5 < 32: using insertion sort\n[ 1, 2, 5, 8, 9 ]\nn=100: using merge sort with insertion for runs\n[ 0, 1, 2, 3, 5 ]" }
  ],
  exercises: [
    { level: 1, title: "Numeric sort", problem: "Write `numSort(arr)` returning a new sorted array of numbers (ascending) without mutating the original.", hints: ["[...arr].sort((a,b)=>a-b)"], solution: "const numSort=arr=>[...arr].sort((a,b)=>a-b);\nconst a=[3,1,4,1,5,9];console.log(numSort(a),a);" },
    { level: 2, title: "Merge sorted arrays", problem: "Write `mergeSorted(a, b)` merging two sorted arrays into one sorted array without sorting.", hints: ["Two pointers i=0,j=0. Push smaller element, advance its pointer."], solution: "function mergeSorted(a,b){const r=[];let i=0,j=0;while(i<a.length&&j<b.length)r.push(a[i]<=b[j]?a[i++]:b[j++]);return[...r,...a.slice(i),...b.slice(j)];}\nconsole.log(mergeSorted([1,3,5],[2,4,6]));" },
    { level: 3, title: "Insertion sort", problem: "Implement `insertionSort(arr)` that sorts in place using insertion sort.", hints: ["For each i>0: pick arr[i] as key. Shift arr[j] right while j>=0 && arr[j]>key. Insert key."], solution: "function insertionSort(arr){for(let i=1;i<arr.length;i++){const k=arr[i];let j=i-1;while(j>=0&&arr[j]>k){arr[j+1]=arr[j];j--;}arr[j+1]=k;}return arr;}\nconsole.log(insertionSort([64,34,25,12,22]));" }
  ],
  interview: [
    { q: "Why is merge sort preferred for linked lists but quick sort for arrays?", a: "Merge sort accesses elements sequentially — perfect for linked lists (no random access). Quick sort requires random access for efficient partitioning and benefits from cache locality in arrays. Quick sort is faster in practice for arrays due to cache-friendly sequential access patterns." },
    { q: "What is a stable sort?", a: "A stable sort preserves the relative order of equal elements. If two records have the same sort key, they appear in the original order. Merge sort is stable; quick sort and heap sort are not. Stability matters when sorting by multiple keys (sort by last name, then first name)." },
    { q: "What is the JavaScript Array.sort() default behavior?", a: "By default, Array.sort() converts elements to strings and sorts lexicographically. [10,9,2].sort() gives [10,2,9] — wrong for numbers. Always provide a comparator function: sort((a,b) => a-b) for ascending numeric sort." }
  ],
  realWorld: [
    { company: "V8 JavaScript Engine", text: "V8 uses TimSort (a hybrid of merge sort and insertion sort) for Array.sort(). Insertion sort handles small arrays (<32 elements) efficiently; merge sort handles larger ones. This is why Array.sort() is O(n log n)." },
    { company: "Database ORDER BY", text: "PostgreSQL uses merge sort for large sorts that don't fit in memory (external sort), and quicksort for in-memory sorts. Understanding sorting helps predict ORDER BY performance and when adding an index eliminates the sort entirely." }
  ],
  quiz: [
    { q: "JavaScript's default Array.sort() sorts:", options: ["Numerically", "Lexicographically (as strings)", "By reference", "Randomly"], answer: 1 },
    { q: "What is merge sort's time complexity?", options: ["O(n²)", "O(n)", "O(n log n)", "O(log n)"], answer: 2 },
    { q: "Which sort is NOT stable?", options: ["Merge Sort", "Insertion Sort", "Bubble Sort", "Quick Sort"], answer: 3 },
    { q: "Bubble sort is best for:", options: ["Production code", "Large datasets", "Learning/educational purposes", "Linked lists"], answer: 2 },
    { q: "To sort numbers correctly in JS: [3,1,2].sort(...):", options: ["No comparator needed", "(a,b) => a-b", "(a,b) => a", "() => 1"], answer: 1 }
  ]
});

// MODULE 56 — Trees and Graphs
window.CSFA_RAW_TOPICS.push({
  id: 'm56-trees-graphs', module: 56, title: 'Trees, Graphs & Traversal',
  tagline: 'Non-linear data structures — BST, BFS, DFS, and real-world graph problems.',
  readMinutes: 7,
  intro: {
    whatItIs: "Trees are hierarchical structures: each node has a parent and children. Binary trees: each node has 0-2 children. Binary Search Trees (BST): left < node < right, enabling O(log n) search. Graphs: nodes (vertices) connected by edges. BFS (Breadth-First Search) and DFS (Depth-First Search) are the fundamental traversal algorithms.",
    whyItMatters: "Trees and graphs model real-world problems: file systems (tree), social networks (graph), HTML DOM (tree), route finding (graph), org charts (tree), dependency resolution (DAG). They appear in almost every technical interview.",
    whereUsed: "File systems, DOM, org charts, React component tree, Trie for autocomplete, decision trees in ML, graph databases (Neo4j), network routing (Dijkstra's), social graphs.",
    commonMistakes: "Forgetting base cases in recursive tree traversal (null node). Always check `if (!node) return` as the first line of recursive tree functions."
  },
  visual: { caption: "BST: left < parent < right. BFS: level by level. DFS: depth-first (pre/in/post order).", type: "trees-graphs" },
  examples: [
    { difficulty: "very-easy", title: "Binary tree node", explanation: "A binary tree is built from Node objects with left and right children.", code: "class TreeNode {\n  constructor(val) { this.val = val; this.left = null; this.right = null; }\n}\nconst root = new TreeNode(10);\nroot.left = new TreeNode(5);\nroot.right = new TreeNode(15);\nroot.left.left = new TreeNode(3);\nroot.left.right = new TreeNode(7);\nconsole.log('Root:', root.val);\nconsole.log('Left:', root.left.val, '| Right:', root.right.val);", language: "javascript", output: "Root: 10\nLeft: 5 | Right: 15" },
    { difficulty: "easy", title: "In-order BST traversal", explanation: "In-order traversal (left → root → right) of a BST yields sorted values.", code: "function inOrder(node, result = []) {\n  if (!node) return result;\n  inOrder(node.left, result);\n  result.push(node.val);\n  inOrder(node.right, result);\n  return result;\n}\nclass N { constructor(v,l=null,r=null){this.val=v;this.left=l;this.right=r;} }\nconst bst = new N(10, new N(5, new N(3), new N(7)), new N(15, new N(12), new N(20)));\nconsole.log('In-order (sorted):', inOrder(bst));", language: "javascript", output: "In-order (sorted): [ 3, 5, 7, 10, 12, 15, 20 ]" },
    { difficulty: "medium", title: "BFS — level-by-level traversal", explanation: "BFS uses a queue to visit nodes level by level. Finds shortest path in unweighted graphs.", code: "function bfs(root) {\n  if (!root) return [];\n  const levels = [], queue = [root];\n  while (queue.length) {\n    const levelSize = queue.length;\n    const level = [];\n    for (let i=0; i<levelSize; i++) {\n      const node = queue.shift();\n      level.push(node.val);\n      if (node.left) queue.push(node.left);\n      if (node.right) queue.push(node.right);\n    }\n    levels.push(level);\n  }\n  return levels;\n}\nclass N{constructor(v,l=null,r=null){this.val=v;this.left=l;this.right=r;}}\nconsole.log(bfs(new N(1,new N(2,new N(4),new N(5)),new N(3))));", language: "javascript", output: "[ [ 1 ], [ 2, 3 ], [ 4, 5 ] ]" },
    { difficulty: "medium-plus", title: "DFS on graph", explanation: "DFS explores as deep as possible before backtracking. Uses recursion or a stack.", code: "function dfsGraph(graph, start, visited = new Set()) {\n  visited.add(start);\n  console.log('Visiting:', start);\n  for (const neighbor of (graph[start] || [])) {\n    if (!visited.has(neighbor)) dfsGraph(graph, neighbor, visited);\n  }\n}\nconst graph = { A:['B','C'], B:['D'], C:['D','E'], D:['E'], E:[] };\ndfsGraph(graph, 'A');", language: "javascript", output: "Visiting: A\nVisiting: B\nVisiting: D\nVisiting: E\nVisiting: C" },
    { difficulty: "hard", title: "Shortest path (BFS)", explanation: "BFS finds the shortest path in unweighted graphs — first time you reach a node is the shortest path.", code: "function shortestPath(graph, start, end) {\n  const queue = [[start, [start]]];\n  const visited = new Set([start]);\n  while (queue.length) {\n    const [node, path] = queue.shift();\n    if (node === end) return path;\n    for (const neighbor of (graph[node] || [])) {\n      if (!visited.has(neighbor)) {\n        visited.add(neighbor);\n        queue.push([neighbor, [...path, neighbor]]);\n      }\n    }\n  }\n  return null;\n}\nconst g = { A:['B','C'], B:['D'], C:['D','E'], D:['F'], E:['F'], F:[] };\nconsole.log(shortestPath(g, 'A', 'F')?.join(' → '));", language: "javascript", output: "A → C → E → F" },
    { difficulty: "real-world", title: "Trie for autocomplete", explanation: "A Trie (prefix tree) enables O(k) prefix lookups — the foundation of autocomplete.", code: "class Trie {\n  constructor() { this.children = {}; this.isEnd = false; }\n  insert(word) {\n    let node = this;\n    for (const c of word) {\n      if (!node.children[c]) node.children[c] = new Trie();\n      node = node.children[c];\n    }\n    node.isEnd = true;\n  }\n  startsWith(prefix) {\n    let node = this;\n    for (const c of prefix) {\n      if (!node.children[c]) return false;\n      node = node.children[c];\n    }\n    return true;\n  }\n}\nconst t = new Trie();\n['apple','app','application','apply'].forEach(w=>t.insert(w));\nconsole.log('app?', t.startsWith('app'));\nconsole.log('ap?', t.startsWith('ap'));\nconsole.log('xyz?', t.startsWith('xyz'));", language: "javascript", output: "app? true\nap? true\nxyz? false" }
  ],
  exercises: [
    { level: 1, title: "Tree height", problem: "Write `height(node)` returning the height of a binary tree (number of edges on the longest path from root to leaf).", hints: ["Base case: !node → return -1 (or 0 for leaf). Return 1 + Math.max(height(left), height(right))."], solution: "function height(n){if(!n)return-1;return 1+Math.max(height(n.left||null),height(n.right||null));}\nclass N{constructor(v,l=null,r=null){this.val=v;this.left=l;this.right=r;}}\nconsole.log(height(new N(1,new N(2,new N(3)),new N(4))));" },
    { level: 2, title: "Graph adjacency list", problem: "Write `buildGraph(edges)` where edges is [[from,to],...]. Return an adjacency list as Map<node, [neighbors]>.", hints: ["For each [from,to]: add to to map[from], add from to map[to] (undirected)."], solution: "function buildGraph(edges){const g=new Map();edges.forEach(([f,t])=>{if(!g.has(f))g.set(f,[]);if(!g.has(t))g.set(t,[]);g.get(f).push(t);g.get(t).push(f);});return g;}\nconsole.log([...buildGraph([['A','B'],['B','C'],['A','C']])]);" },
    { level: 3, title: "Has cycle", problem: "Write `hasCycle(graph, start)` using DFS to detect if an undirected graph has a cycle.", hints: ["DFS with visited set. Track parent to avoid false cycle with parent edge."], solution: "function hasCycle(g,s){const v=new Set();function dfs(n,p){v.add(n);for(const nb of(g[n]||[])){if(!v.has(nb)){if(dfs(nb,n))return true;}else if(nb!==p)return true;}return false;}return dfs(s,null);}\nconsole.log(hasCycle({A:['B'],B:['A','C'],C:['B','A']},'A'));" }
  ],
  interview: [
    { q: "When would you use BFS vs DFS?", a: "BFS: when you need the shortest path (unweighted), level-by-level traversal, or find if any solution exists within minimum steps. DFS: when you need to explore all paths, detect cycles, topological sort, or solve maze/backtracking problems. BFS uses more memory (queue grows wide); DFS uses less (call stack depth)." },
    { q: "What is a Binary Search Tree?", a: "A BST is a binary tree where: left subtree contains only nodes with values less than the node's value, right subtree contains only values greater. This enables O(log n) search, insert, and delete for balanced trees. If unbalanced (degenerate/skewed), operations degrade to O(n)." },
    { q: "What is topological sort used for?", a: "Topological sort orders nodes of a Directed Acyclic Graph (DAG) so that for every edge (u→v), u comes before v. Used for: task scheduling with dependencies, build systems (which module to compile first), package dependency resolution (npm install order)." }
  ],
  realWorld: [
    { company: "Google Maps", text: "Google Maps uses Dijkstra's algorithm (weighted BFS/shortest path) for route finding on a massive graph of roads (nodes) and distances (edge weights). Real-time traffic is incorporated as dynamic edge weights." },
    { company: "npm / package managers", text: "npm builds a Directed Acyclic Graph of package dependencies and runs topological sort to determine installation order. Circular dependencies break topological sort — npm handles them specially." }
  ],
  quiz: [
    { q: "In a BST, where are smaller values stored?", options: ["Right subtree", "Left subtree", "Root only", "Random"], answer: 1 },
    { q: "BFS uses which data structure?", options: ["Stack", "Queue", "Array", "Heap"], answer: 1 },
    { q: "DFS uses which data structure?", options: ["Queue", "Stack (or recursion)", "Map", "Set"], answer: 1 },
    { q: "In-order traversal of BST gives:", options: ["Random order", "Descending order", "Sorted ascending order", "Level-by-level"], answer: 2 },
    { q: "What is a Trie used for?", options: ["Graph traversal", "Sorting", "Prefix/autocomplete lookups", "Hash table collision"], answer: 2 }
  ]
});
